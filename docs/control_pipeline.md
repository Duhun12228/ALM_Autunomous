# 자율주행 제어 파이프라인 — 구조 · 파라미터 · 검증

> **이 문서는** 목표 pose 하나가 바퀴 명령이 되기까지의 전 구간을 코드 기준으로
> 정리한 레퍼런스입니다. 이 프로젝트를 처음 보는 사람도 읽을 수 있게 전제부터 씁니다.
>
> 기준: `feat/nav2-hybrid-astar`, 2026-08-20
> 선행 문서: `docs/control_feedback_analysis.md` (문제 발견 단계의 기록)

---

## 0. 5분 요약

### 이 로봇의 제약 두 가지

**① STM32 업링크가 없습니다.** `uart_protocol.md` v2 State 섹션은 '미구현'입니다.
실측 조향각도, 측정 속도도, MCU fault 도 올라오지 않습니다. 로봇이 자기 상태에 대해
가진 정보는 `/Odometry.pose`(FAST-LIO, twist 는 0)와 `/imu/data`(원시 자이로) 둘뿐입니다.

**② `normal` 모드는 자동차형입니다.** 최소 선회반경 `R_min = 1.643 m`. 제자리 회전은
`spin` 모드에서만 되고, 그건 STM32 고정 자세(47°)를 씁니다.

그리고 전역 플래너는 `R_min` 원호와 직선만 이어 붙이므로 **제자리 회전을 표현하지
못합니다.** 즉 4륜 독립조향의 핵심 능력이 계획 단계에서는 보이지 않습니다.
이 공백을 제어단에서 메우는 것이 `ALIGN` 입니다 (§6.8).

### 그래서 설계가 이렇게 됐습니다

| 축 | 접근 |
|---|---|
| **구동 속도** | 개루프로 둔다. 선회반경은 `R = ICR_y(δ)` 라 **조향각만의 함수**이므로, 속도가 20% 틀려도 경로의 '모양'은 같다. 오차는 MPPI 가 위치 피드백으로 메운다 |
| **조향** | 개루프로 두되, **액추에이터가 따라올 수 있는 속도로만 명령한다.** 명령 슬루율 < 실제 슬루율이면 '명령 = 실제'가 성립해 피드백 없이도 모델이 맞는다 |
| **조향각을 모르는 구간** | 기동 직후·모드 전환 중에는 **속도를 0 으로 잡는다** (dwell) |
| **제자리 회전** | 플래너가 표현 못 하므로 **제어단이 `/plan` 을 직접 보고** 헤딩오차가 60° 넘게 벌어지면 스스로 `spin` 을 건다. 목표 헤딩을 래치하는 '기동'이지 모드 분류가 아니다 (§6.8) |

### 실차에 나가기 전 반드시 할 것 셋

1. **`S_정지` 확인** — 지면에 세운 채(잭업 금지) 조향이 도는가, 5초에 60° 도는가,
   스톨하지 않는가. **스톱워치 1분**이면 된다 (§9.1)
2. **STM32 업링크 11바이트 요청** — `status_flags` 3 + `steer_actual` 4 + `ack_seq` 4.
   이 중 3바이트가 지금 죽어 있는 안전장치(`stop_on_mcu_fault`)를 살린다 (§9.2)
3. **`front_x`/`rear_x` 배분 CAD 확인** — 휠베이스 합만 CONS 로 확정됐다 (§9.3)

### 현재 상태 한 줄

시뮬레이션에서 **충돌 0**(총 30+ 회 목표 실행). 좁은 통로 주행은 성립한다.
**`ALIGN`(§6.8) 을 새로 넣었다** — 플래너를 바꾸는 대신 제어단이 `/plan` 을 직접 보고
헤딩을 고치는 방식이다. 경로가 있는데 헤딩만 어긋난 경우는 이걸로 해결됐고(실측 1회,
5.2 s), 걸리지 않는 상황에서는 완전한 무동작이다(A/B 3회, 숫자 소수점까지 동일).

⚠ **다만 "경로가 아예 안 나오는" 실패는 ALIGN 이 못 고친다.** 입력이 없기 때문이다.
그리고 실측 결과 그 실패의 원인은 헤딩이 아니었다 — 시작 헤딩을 0°/−45°/−90° 로
바꿔 봐도 전부 계획 실패였다. 원인은 계획단(코스트맵 팽창·계획시간)이다. §7.3 / §9.3-K.

⚠ 다만 이번에 **하네스의 실행편차가 크다는 것도 확인**했습니다(§8.9). ALIGN 이 한 번도
발동하지 않은 목표가 성공↔실패로 뒤집혔습니다. 그래서 §8 의 "되돌림" 판정 중 일부는
노이즈였을 수 있고, 앞으로의 A/B 는 반복 실행이 필수입니다.

**실차 주행은 미검증**이다 — §7.4 의 가정 목록을 반드시 볼 것.

---

## 1. 이 로봇이 어떤 로봇인가

### 1.1 4륜 독립조향(4WIS), 하지만 STM32 는 3가지 모드만

| 모드 | `mode_id` | 동작 | 제자리 회전 | 조향각 |
|---|---|---|---|---|
| `normal` | 1 | 자동차형. 앞바퀴 조향 + 뒷바퀴 50% 역조향 | ❌ | **연속 제어** (±30°) |
| `crab` | 3 | 4륜 같은 방향 → 게처럼 평행이동 | ❌ | STM32 고정 90° (`CONS(8)`) |
| `spin` | 4 | 제자리 회전 (제로턴) | ✅ | STM32 고정 47° (`CONS(9)`) |

`crab`/`spin` 은 Jetson 이 **방향(부호)과 속도만** 정합니다. 연속적인 방향 제어는
`normal` 에서만 됩니다.

### 1.2 기구 상수 — CONS 가 정답입니다

**`base_control.yaml` = STM32 `ALM07.slx` 의 CONS 벡터**가 단일 진실 공급원입니다.

```
wheelbase B = 1.000 m   (CONS(2))      rws_ratio = 0.5   (CONS(1))
track     T = 0.919 m   (CONS(3))      max_steer = 30°   (내측 전륜)
straight_angle = 2°     (CONS(4))      crab_rpm_scale = 0.5  (CONS(10))
                                       zero_turn_rpm_scale = 0.6 (CONS(11))
```

> URDF 가 한동안 **휠베이스와 윤거를 뒤바꿔** 적고 있었습니다
> (URDF 윤거 1.0000 = CONS 휠베이스 1.000, 정확히 일치). CONS 기준으로 고쳤고,
> `nav2_kinematic_check.py` §6 이 이제 이 둘을 대조합니다.

### 1.3 가장 중요한 파생값: R_min = 1.643 m

```
ICR_y(d) = half_track + B / (tan(d) + tan(rws·d))

d = 30°:  tan(30°) + tan(15°) = 0.5774 + 0.2679 = 0.8453
          1.000 / 0.8453 = 1.1830  +  0.4595  =  1.6425 m
```

후륜 역조향이 없으면 `2.19 m` 입니다 — **역조향 덕에 25% 작아집니다.**

| 조향각 | R | vx=0.20 에서 wz |
|---|---|---|
| 10° | 4.250 m | 0.047 rad/s |
| 20° | 2.310 m | 0.087 rad/s |
| **30°** | **1.643 m** | **0.122 rad/s** |

**조향각은 R 만 정합니다** (속도 무관). 속도가 그 원을 도는 빠르기를 정하고,
둘이 곱해져 `vx = R · wz` 가 됩니다.

### 1.4 이 맵에서 R_min 이 얼마나 제약인가 (alm_lab 실측)

| | 면적 | 연결성분 | 최대 조각 |
|---|---|---|---|
| 로봇이 들어갈 수 있는 곳 (여유≥0.530) | 1429.7 m² | 19 | 99.9% |
| **제자리 회전 가능** (여유≥0.894) | 1297.9 m² | 9 | **96.4%** |
| **R_min U턴 가능** (여유≥2.173) | 956.9 m² | 6 | **50.7%** |

**통행가능 영역의 33.1% 에서는 R_min 만으로 방향을 되돌릴 수 없습니다.**
제자리 회전을 허용하면 그 공간이 96.4% 하나로 이어집니다 — 이게 4WIS 를 쓰는 이유이고,
동시에 `ALIGN`(§6.8)이 필요한 배경입니다.

---

## 2. 전체 파이프라인 — 목표 pose 가 바퀴까지

```
 [사용자] RViz 2D Nav Goal 또는 WebUI
    │
    │  ① 전역 경로계획  (재계획: 3 Hz 또는 경로 무효화 시 즉시)
    ▼
 ComputePathToPose ── GridBased (SmacPlannerHybrid)
    │                   R_min=1.643 프리미티브만 -> 나오는 경로가 실현가능
    │                   ⚠ 제자리 회전은 표현 못 한다 -> 제어단 ALIGN 이 메운다 (§6.8)
    │                   (TightSpace=Lattice 를 시도했다가 비활성 — §8.5)
    │
    │  ② 경로 마감
    ▼
 SmoothPath ────────── ConstrainedSmoother (Ceres). 실패해도 원본 경로로 주행
    │
    │  ③ 경로 추종 (20 Hz)
    ▼
 FollowPath ────────── MPPIController. 1600 궤적 × 6.30 s 지평선 (= 1.26 m @0.20 m/s)
    │                   → /cmd_vel_nav
    │  ④ 속도 평활화 (20 Hz)
    ▼
 velocity_smoother ─── scale_velocities=True 로 vx:wz 비 유지. feedback=OPEN_LOOP
    │                   → /cmd_vel
    │  ⑤ 동작권 중재 (50 Hz)
    ▼
 cmd_arbiter ───────── 자율 vs 텔레옵. → /cmd_vel_mux
    │  ⑥ 모드 해석 + 안전 게이팅 + 기구 응답 + 4WIS 인코딩 (50 Hz)
    ▼
 command_manager ───── auto → normal/spin/crab
    │                   ★ ALIGN: /plan 을 직접 보고 헤딩오차 >60° 면 spin 으로 정정
    │                   |wz| ≤ |vx|/R_min 조향 한계
    │                   기동 정렬 dwell / 모드 전환 dwell
    │                   e-stop(즉시) vs timeout·stale(감속 램프)
    │                   twist → (steer_deg, speed_rpm, mode_id)
    │                   조향 슬루 제한 → wz 역산 재정합
    │                   → /mcu/command
    │  ⑦ 전송 (기구학 없음)
    ▼
 mcu_bridge ────────── UART 115200, 16 B 프레임, CRC16-CCITT → STM32
                       ⚠ 업링크는 미구현 — 단방향이다

 ┌──────────────────────────────────────────────────────────────┐
 │ 관측 (제어 경로 밖, 로깅 전용)                                │
 │ steering_observer  — /Odometry.pose 만으로 κ=Δθ/Δs → 실효 조향각│
 │ (goal_yaw_aligner 는 삭제됨 — ALIGN 이 대체, §6.10-D)          │
 └──────────────────────────────────────────────────────────────┘
```

### ① 전역 플래너 — 왜 Hybrid 인가

일반 격자 A*(SmacPlanner2D)는 로봇을 **점**으로 보고 90° 꺾인 경로를 태연히 냅니다.
Hybrid-A* 는 **R_min 을 만족하는 호(모션 프리미티브)만을 이동 단위로** 쓰므로 나오는
경로가 처음부터 실현가능하고, 경로의 모든 pose 에 **유효한 yaw** 가 있습니다
(MPPI `use_path_orientations` 를 켤 수 있는 이유).

**대가 두 가지**가 있고, 둘 다 같은 뿌리에서 나옵니다.

1. 프리미티브 집합에 **제자리 회전이 없습니다.** "목표 근처에서 큰 자세변화"에 해가
   없습니다.
2. **경로가 항상 현재 로봇 헤딩에 접해서 출발합니다.** 탐색의 시작 상태 θ 가 현재
   헤딩이고 확장이 R_min 원호뿐이기 때문입니다. 즉 벽을 보고 서 있으면 전역경로는
   "벽 쪽으로 나가서 R_min 으로 크게 감아 돌아라" 라고 말하고, **그건 경로 입장에서
   정답입니다.** 고쳐야 할 것은 경로가 아니라 **출발 헤딩**입니다.

   이 성질에는 정량적 따름정리가 하나 있습니다 — 출발 시점에 '경로 헤딩오차' 가
   낼 수 있는 최대값이 호각으로 묶입니다:

   ```
   |경로 헤딩오차|_max = lookahead / R_min
       lookahead 1.00 m ->  34.9°
       lookahead 1.72 m ->  60.0°
       lookahead 3.00 m -> 104.6°
   ```

   ##함정## 그래서 `align_lookahead_m: 1.0` 과 `align_enter_deg: 60°` 조합은
   **출발 시점에 수학적으로 절대 안 걸립니다.** 값은 멀쩡해 보이는데 기능이
   죽어 있던 기간이 있었습니다 — 경위와 수정은 §6.11.

**해결한 방식**: 플래너가 회전을 표현하게 만드는 대신, **제어단이 경로를 직접 보고
헤딩을 고치게** 했습니다(`ALIGN`, §6.8). 플래너는 손대지 않으므로 Hybrid-A* 의 장점
(실현가능한 경로 · 유효한 yaw)이 그대로 남습니다.

> 이를 풀려고 `SmacPlannerLattice`(회전 프리미티브 포함)를 도입했다가 되돌렸습니다.
> control set 은 만들어 검증해 뒀습니다 —
> `alm_navigation/lattice_primitives/` (408개, 회전 32개, 최소반경 1.677 ≥ R_min).
> 경위와 수치는 §8.5.

### ③ MPPI — 왜 Ackermann 인가 (DiffDrive 에서 전환)

> 이 절은 2026-08-23 에 뒤집혔습니다. 예전 근거("`spin` 이 되는 로봇이니 MPPI 를
> 묶지 말자")를 아래에 남겨 두었습니다.

**실차 증상**: 직진해도 되는 길에서 좌우로 계속 와리가리.

원인은 **MPPI 의 모델과 실제 기구가 달랐던 것**입니다.

| | 값 |
|---|---|
| MPPI(DiffDrive)가 낼 수 있다고 믿은 wz | `wz_max` (당시 0.45) |
| `normal` 모드가 실제로 내는 wz | \|vx\|/R_min = **0.122** @vx=0.20 |
| 간극 | **3.7배** |

`command_manager` 가 조용히 클램프하는데 MPPI 는 그 사실을 모릅니다. "wz=0.35 를
6.3 s 주면 경로에 붙는다"고 롤아웃해 놓고 실제로는 1/3.7 만 돌아, 다음 틱에 더 큰
wz 를 내고, 결국 경로를 넘겨 반대로 꺾는 **진동**이 됩니다. `PathAlignCritic`
가중치 14(최대)가 이를 증폭합니다.

Ackermann 모델은 롤아웃 단계에서 `|wz| <= |vx| / min_turning_r` 을 그대로 적용합니다
— `command_manager` 의 클램프와 **같은 식**입니다. 모델과 기구가 일치하므로 클램프로
잘려나가는 명령이 사라집니다. `wz_std` 도 0.20 -> 0.07 로 함께 줄였습니다(0.20 이면
표본의 54% 가 실행 불가능한 회전율이었습니다).

**예전 근거가 왜 더는 유효하지 않은가**: "Ackermann 으로 묶으면 제자리 회전을 계획
못 해 자세 정렬을 전부 리커버리에 떠넘긴다" 였는데, 그 뒤 **ALIGN**(§6.8)이 들어와
그 두 가지를 명시적으로 가져갔습니다 — 출발 시 정렬은 §6.11 의 pre-align, 목표 자세
정렬은 `align_min_remaining_m: 0.0`. MPPI 가 우연히 내는 회전에 기대던 구조에서
벗어난 것이라 오히려 낫습니다.

**따름정리 하나**: Ackermann 에서 MPPI 의 실효 wz 상한은 0.122 이고
`auto_spin_angular_threshold` 는 0.20 이므로, **MPPI 발 spin 라우팅은 이제 구조적으로
발생하지 않습니다.** spin 을 거는 주체는 ALIGN 과 BT 리커버리 Spin(0.25) 둘뿐입니다.
이건 의도한 것입니다 — 라우팅이 '우연히 생긴 명령 모양' 이 아니라 명시적 판단으로만
일어납니다.

`PathAlignCritic.use_path_orientations: true` (가중치 14, 최대)가 Hybrid/Lattice 전환의
실질 이득입니다 — 경로의 모든 pose 에 유효한 yaw 가 있어야 켤 수 있습니다.

### ⑥ command_manager — 50 Hz 틱 순서

```
 1. 모드 해석        auto → normal/spin/crab (히스테리시스)
 1b. ALIGN 기동      주행 중: /plan 헤딩오차 > 60° 가 0.6 s 지속
                    출발 구간(이동 < 0.30 m): 목표 방위각오차 > 40° → pre-align
                    → 목표 헤딩 래치, spin 강제 (§6.8, §6.11)
                     (게이팅 판정을 먼저 계산해 dwell 이 타임아웃을 안 먹게 한다)
 2. dwell 게이팅     기동 정렬 / 모드 전환 중이면 **전 성분 0**
 3. 모드별 제약      spin: 병진 0 / crab: wz=0 / normal: vy=0
 4. 속도 클램프      vx[-0.07, 0.20], vy±0.30, wz±0.45
 5. 조향 한계        |wz| ≤ |vx|/R_min,  |vx|<0.03 이면 wz=0
 6. 정지 판정        emergency(즉시) / soft(감속 램프)
 7. 가속 제한
 8. 조향 한계 재적용 (가속 램프에서 wz/vx 비가 깨지므로)
 9. 4WIS 인코딩      twist → (steer_deg, speed_rpm, mode_id)
10. 조향 슬루 제한   변화율 제한 → 그 각도로 나올 wz 를 역산해 cmd_vel 재정합
```

### ⑦ UART v2 — 함정 둘

```
프레임 (16 B): 0xAA 0x55 | msg_type | length | payload(10) | crc16(big-endian)
Command:  <ffBB = steer_deg(f32), speed_rpm(f32), mode_id(u8), flags(u8)
```

1. **부호 반전** — STM32 가 `normalDriveMode(-steer_deg, ...)` 로 호출하므로
   **좌회전(wz>0)에 음수 조향각**을 보냅니다.
2. **`speed_rpm` 은 차체 속도가 아니라 내측 전륜 rpm** — 나머지 3륜은 STM32 가 스케일.

---

## 3. 기구 상수가 어디에 쓰이는가

```
 [STM32 CONS = base_control.yaml]          [CAD = URDF]
  B 1.000 · T 0.919 · rws 0.5               wheel_r 0.103 · body 1.2×0.99
  max_steer 30°                             front_x 0.6106 · rear_x -0.3894
        │                                          │
  fourwis_encode.min_turn_radius()                 │
        ▼                                          ▼
   R_min = 1.643 m                          footprint x[-0.65,0.72] y[±0.53]
   wz_max = 0.122 @0.20                     내접 0.530 / 외접 0.894
        │                                          │
   ┌────┼──────────┬───────────┬──────────┐   ┌────┴────┐
   ▼    ▼          ▼           ▼          ▼   ▼         ▼
 Hybrid Lattice  Smoother  command_mgr  관측기 costmap  CostCritic
 R_min  R_min     R_min    |wz|≤|vx|/R  κ→δ  inflation (충돌검사)
                                              > 0.894
```

**`inflation_radius` 는 반드시 외접(0.894)보다 커야** costmap 충돌평가와 MPPI
`CostCritic` 임계값이 성립합니다. (local 1.00 / global 1.20 ✓)

`wheel_radius` 는 유일하게 rpm 환산에만 쓰입니다: `rpm = v/(2πr) × 60 × gear_ratio`.

---

## 4. 피드백 현황 — 무엇이 닫혀 있나

| 피드백 | 신호 | 소비자 | 상태 |
|---|---|---|---|
| **위치·자세** | FAST-LIO `/Odometry.pose` | MPPI(20 Hz), 재계획(3 Hz) | ✅ **닫힘** |
| **속도·요레이트** | `/Odometry.twist` | MPPI `robot_speed` | ❌ **항상 0** |
| **실제 조향각** | (STM32 업링크) | — | ❌ **신호 자체가 없음** |
| **조향 (모델)** | `command_manager.out_steer_deg` | 자신 | ⚠ 개루프 모델 |
| **조향 (관측)** | `steering_observer` κ=Δθ/Δs | 로깅 전용 | ⚠ 주행 중에만 |

**핵심 논리**: 슬루 제한값을 실제 액추에이터보다 **낮게** 잡으면 액추에이터가 항상
여유롭게 따라오므로 `δ_actual ≈ δ_cmd` 가 성립합니다. 시뮬레이션 실측으로
**명령 vs 실제 간극 평균 0.010°** 를 확인했습니다.

**업링크 없이도 주행은 성립합니다** (`expect_mcu_state: false` 가 이를 명시):

| 경로 | 업링크 없을 때 |
|---|---|
| odom 워치독 | `/Odometry`(FAST-LIO)를 본다 ✅ |
| `stop_on_mcu_fault` | `mcu_fault` False 로 남아 아무것도 안 막음 ✅ |
| e-stop 래치 해제 | 정상 ✅ |
| `/joint_states` | 미발행 — RViz 바퀴 표시만 빠짐 |

대가는 하나: **MCU fault 를 알 수 없습니다.**

---

## 5. 파라미터 레퍼런스

숫자 하나를 바꿀 때 **같이 봐야 하는 것**을 표로 묶었습니다.
`ros2 run alm_navigation nav2_kinematic_check.py` 가 이 관계들을 자동 검사합니다.

### 5.1 기구 상수 (`base_control.yaml`) — 바꾸면 거의 전부가 따라 움직임

| 파라미터 | 값 | 출처 | 바꾸면 함께 |
|---|---|---|---|
| `wheelbase_m` | 1.000 | CONS(2) | R_min → 플래너·스무더·lattice 재생성·URDF |
| `track_m` | 0.919 | CONS(3) | 〃 |
| `rws_ratio` | 0.5 | CONS(1) | 〃 |
| `max_steer_deg` | 30.0 | ##CONFIRM## | 〃 |
| `wheel_radius_m` | 0.103 | ##CONFIRM## | rpm 환산만 |
| `gear_ratio` | 1.0 | ##CONFIRM## | 〃 |

### 5.2 속도·조향 (한 세트로 움직임)

| 파라미터 | 값 | 유도 |
|---|---|---|
| `max_linear_x` | **0.20** | 실측 전 안전값. **세 곳**(base_control / MPPI `vx_max` / velocity_smoother)이 같아야 함 |
| `min_linear_x` | **-0.07** | 전후진 비 유지 → `reverse_penalty 3.0` 근거 |
| `max_steer_rate_deg_s` | **20.0** | `max_steer_rate/vx = 100 deg/m` 유지가 핵심. 0°→30° 진입 0.300 m |
| `max_angular_z` | 0.45 | spin 상한과 일치 |

> **핵심 관계**: 경로 품질을 정하는 것은 `max_steer_rate / max_linear_x` 비(deg/m)이지
> 각각의 절대값이 아닙니다. 비를 유지한 채 둘을 같이 낮추면 **경로 모양은 그대로**이고
> 시간만 늘어나며, 슬루율 절대값이 낮아져 '명령 < 실제'가 보장됩니다.
> 실측 후 올릴 때도 **비를 유지**하세요.

### 5.3 dwell — 조향각을 모르는 구간을 시간으로 메움

| 파라미터 | 값 | 커버하는 조건 |
|---|---|---|
| `startup_steer_align_sec` | **5.0** | 전체 가동범위 60°를 `S_정지 ≥ 12 deg/s` 로 커버 |
| `mode_switch_dwell_sec` | **4.0** | `normal↔spin` 77° 스윕. 3.0 → 4.0 으로 올렸습니다 |

`mode_switch_dwell_sec` 는 **기구학이 값을 정합니다**:

```
최악 스윕 = normal 반대쪽 풀락(30°)  →  spin 제로턴 자세(47°, CONS(9))  =  77°
77° ÷ max_steer_rate_deg_s(20)  =  3.85 s      →  3.0 s 는 부족했다
```

부족하면 조향축이 아직 도는 중에 바퀴가 굴러 **의도와 전혀 다른 궤적**이 납니다
(§6.2 와 같은 부류의 버그). `ALIGN`(§6.8)이 `normal↔spin` 전환을 더 자주 만들므로
이 미달이 더 중요해졌습니다. `nav2_kinematic_check.py` §5 가 이 관계를 검사합니다.

> ⚠ `crab`(90°) 전환은 `30+90=120° ÷ 20 = 6.0 s` 가 필요합니다. 자율주행에서 `crab`
> 은 쓰이지 않으므로 덮지 않았습니다. **수동 crab 을 실제로 쓸 거면 6.0 으로 올리세요.**

둘 다 `vx=0` 구간이라 **저속 주행 전략이 도와주지 못합니다.** `S_정지` 실측이 필요한
유일한 이유입니다(§9.1).

### 5.4 모드 전환 (`auto` 상태머신)

| 파라미터 | 값 | 유도 |
|---|---|---|
| `auto_spin_angular_threshold` | 0.35 | ⚠ 0.10 으로 유도했다가 되돌림 — §8.6. 실질적으로 'BT 리커버리 전용' 문턱 |
| `auto_spin_linear_threshold` | 0.04 | **실질 가드.** 정상 주행(lin≈0.20)은 여기서 걸러짐 |
| `auto_spin_exit_linear_threshold` | 0.10 | `max_linear_x` 가 이 값의 2배 이상이어야 함 → **vx_max 0.20 이 하한** |
| `auto_mode_min_hold_sec` | 0.80 | ⚠ 4.0 으로 올렸다가 되돌림 (§8) |

> 위 넷은 전부 **Nav2 가 낸 twist 의 모양을 사후 분류**하는 값입니다. 이것만으로는
> spin 이 사실상 BT 리커버리 전용이 됩니다 — 자세한 이유와 대응은 §6.8.

### 5.4b ALIGN — 경로 헤딩 정렬 기동 (§6.8)

| 파라미터 | 값 | 유도 |
|---|---|---|
| `align_enabled` | true | 끄는 비용은 '경로 헤딩 정정'이 사라지는 것. 켜는 비용은 0 (§7.3 A/B) |
| `align_lookahead_m` | 1.0 | 짧으면 노이즈에 민감, 길면 코너를 미리 보고 과하게 돎. **주행 중 값은 늘리지 말 것** — 아래 실측 분포가 전부 이 값 기준 |
| `align_lookahead_m_stopped` | **3.0** | 출발 구간 전용. 경로가 내 헤딩에 접해서 출발하므로 오차 상한이 `lookahead/R_min` 이다. 1.0 m 면 34.9° 라 `align_enter_deg`(60°)에 **닿지도 못한다** (§6.11) |
| `align_enter_deg` | **60.0** | **실측 분포로 결정.** alm_lab 264 s 주행에서 \|헤딩오차\| p50 8.8° · p90 39.8° · p99 67.2° · max 129.9°. 60° + 0.6 s 지속 조건에서 진입 **2회**뿐 |
| `align_exit_deg` | 15.0 | ★ 반드시 `< align_enter_deg`. 같거나 크면 매 틱 왕복 (기동기가 생성 시점에 거부함) |
| `align_enter_hold_sec` | 0.6 | 재계획 순간의 튐을 거름. 실측에서 45° 초과 연속구간 6개 중 0.6 s 이상은 3개 |
| `align_max_sec` | 25.0 | 0.25 rad/s 로 180° 가 12.6 s. 여유 2배. 재래치가 켜져 있으면 이 값이 **1회전(358°) 상한**도 겸한다 |
| `align_cooldown_sec` | 3.0 | 왕복 방지 마지막 방벽 |
| `align_kp` | **0.6** | `wz = clamp(kp·오차, wz_min, auto_spin_max_angular_speed)`. 1.2 였을 때 포화가 풀리는 지점이 오차 12.0° 로 이탈 문턱(15°)보다 안쪽 = **기동 내내 최고속도로 돌다 갑자기 멈춤**. 0.6 이면 23.9° 부터 비례 감속 |
| `align_wz_min` | 0.15 | 마지막 몇 도에서 회전이 멈춰버리지 않게 |
| `align_min_remaining_m` | 0.0 | 0 = 목표 직전 최종 자세정렬도 ALIGN 이 담당. 실측 진입 2회가 모두 남은경로 3 m 근처였으므로 0 이 맞음 |
| `align_plan_stale_sec` | 2.0 | `/plan` 이 끊기면 '주행 중 아님'으로 봄 (Nav2 는 목표 수행 중에만 발행) |

**출발 구간 전용 (§6.11)**

| 파라미터 | 값 | 유도 |
|---|---|---|
| `align_start_travel_m` | **0.30** | '출발 구간' 의 정의. ★ **속도가 아니라 이동거리다.** 목표 수락 후 가속 1.0 m/s² 라 0.15 s 만에 vx=0.15 가 되는데 진입 지속시간이 바로 그 0.15 s 라, 속도로 정의하면 창이 사실상 0 이다 (통합 시험에서 한 번도 안 걸렸다). 0.30/0.15 = **2.0 s 의 실효 창** |
| `align_enter_deg_stopped` | **60.0** | 25.0 에서 올림. lookahead 가 3.0 m 가 되어 상한이 104.6° 로 열렸으므로, 25° 를 두면 정상적인 S자 출발에도 매번 걸린다(3 m 앞 접선이 20~30°). 60° 는 3 m 구간에서 최대곡률의 57% 를 유지해야 나오는 값 |
| `align_enter_hold_sec_stopped` | 0.15 | `/plan` 첫 발행 직후의 튐만 거르면 된다. 정지 전용 조건의 실체는 '문턱' 이 아니라 **짧은 지속시간 + 긴 lookahead** 다 |
| `align_relatch_stopped` | true | 출발 구간에는 왕복 위험이 없으므로 목표 헤딩을 갱신해 **한 번의 spin 으로** 끝낸다. 끄면 한 기동이 도는 각도가 '래치각 − 이탈각' 으로 묶인다 |
| `align_goal_bearing_enabled` | true | '벽을 보고 선 채 뒤쪽 목표' 를 뚫는 유일한 경로. 경로 대신 **목표까지의 직선 방위각**을 오차로 쓴다 |
| `align_goal_bearing_deg` | 40.0 | 이보다 작으면 R_min 으로 펴면서 가는 편이 빠르다(40° 를 펴는 호길이 = 1.15 m) |
| `align_goal_bearing_min_dist_m` | 1.5 | 이보다 가까우면 방위각이 노이즈에 민감 |
| `align_goal_jump_m` | 1.0 | `/plan` 끝점이 이만큼 튀면 '새 목표'. 플래너 `tolerance`(0.50 m) 안에서 끝점이 흔들리므로 넉넉히 |

**주행 중 진입 문턱을 낮추면 안 되는 이유**: 실측에서 30° 초과는 전체 틱의 **35.1%**
입니다. 정상 선회가 전부 걸려 `vx` 가 무너집니다 — §8.1 의 `min_hold 4.0` 실험과 같은
실패입니다. 반대로 **출발 구간에서는 그 위험이 없습니다** — 아직 안 굴러갔으므로
오인해서 잃을 것이 없고, 창이 목표당 한 번만 열립니다.

### 5.5 Nav2 계획·제어

| 파라미터 | 값 | 유도 |
|---|---|---|
| ~~`TightSpace.*`~~ | — | **현재 비활성**(`planner_plugins` 에 없음). 설정은 보존 — §8.5 |
| `PathExpiringTimer seconds` | 0.33 | 재계획 주기 ≤ costmap 갱신률(3 Hz). 낡은 경로 이동거리 = vx/f |
| `global_costmap.update_frequency` | 3.0 | 위와 짝 |
| `MPPI time_steps × model_dt` | 63 × 0.10 | **곱 × vx_max = 1.26 m** 를 유지하는 것이 규칙 |
| `MPPI vx_std` | 0.09 | 제어 범위(0.27)의 1/3 |
| `inflation_radius` | 1.00 / 1.20 | **반드시 외접 0.894 초과** |

### 5.6 안전

| 파라미터 | 값 | 비고 |
|---|---|---|
| `estop_latch` | true | 해제는 `/emergency_stop/release` 서비스로만 |
| `hard_stop_on_timeout` | false | e-stop=즉시 / timeout·stale=감속 램프 |
| `stop_on_mcu_fault` | true | ⚠ 업링크가 없어 **현재 동작하지 않음** |
| `expect_mcu_state` | false | 업링크 미구현이 전제임을 명시. 구현되면 true |

---

## 6. 이번 작업으로 바뀐 것

시간순이 아니라 **주제별 최종 상태**입니다. 실패한 시도는 §8 에 따로 모았습니다.

### 6.1 조향을 "따라올 수 있는 속도로만" 명령 🔴

**문제**: `command_manager` 는 `vx/vy/wz` 에만 가속 제한을 걸었습니다. 그런데 normal
모드 조향각은 **비율 `wz/vx`** 로 결정되므로 성분별 제한은 이 비를 전혀 묶지 못합니다.
게다가 `|wz| ≤ |vx|/R_min` 클램프가 걸리면 `R = R_min` 이 되는데 이는 **정의상 최대
조향각**입니다. 결과적으로 **모든 출발 선회에서 풀락**을 때렸습니다.

```
틱 루프 재현 (목표 vx=0.45, wz=0.20 — 완전히 실현가능한 요청)
  t=0.04  steer 0° → -30°   슬루 1500 deg/s   ← 어떤 조향 모터도 못 따라감
```

**조치**: `fourwis_encode` 에 `slew_limit()` 과 `wz_from_steer()`(encode 의 역함수)를
추가하고, 틱 마지막에 조향각을 제한한 뒤 **그 각도에서 나올 `wz` 를 역산해
`cmd_vel` 을 재정합**합니다. 재정합이 핵심입니다 — 조향각만 깎으면 한 메시지 안의 두
필드가 서로 다른 이야기를 합니다.

**실측 검증** (2,624 샘플, 메시지 스탬프 기준):

| | 결과 |
|---|---|
| `cmd_vel.wz` ↔ `steer_deg` 정합 | 최대오차 **2.6e-09 rad/s** |
| 조향 슬루 제한 (설정 20 deg/s) | p99 **20.45** deg/s |
| **명령 vs 실제 조향 간극** | 평균 **0.010°**, 최대 0.406° |

세 번째가 설계 전제("명령 슬루율 < 실제 슬루율이면 명령 = 실제")의 실증입니다.

### 6.2 조향각을 모르는 구간은 멈춰서 기다림 🔴

전원 투입 시 조향각을 알 방법이 없고(업링크 없음), `vx=0` 이라 어떤 관측기도 못
씁니다. 모드 전환 시에는 STM32 고정 자세가 달라(normal ±30° / spin 47° / crab 90°)
조향축이 크게 스윕합니다.

```yaml
startup_steer_align_sec: 5.0    # 직진(0°)+정지 유지. S_정지 ≥ 12 deg/s 커버
mode_switch_dwell_sec: 3.0      # 속도 0, mode_id 는 먼저 보내 STM32 가 미리 조준
```

> 🔴 **dwell 이 `spin` 에서 회전을 안 막던 버그**가 있었습니다 —
> `if effective == "normal"` 조건 때문. spin 의 회전은 바퀴 구동으로 만들어지므로
> 조향축이 47° 로 가는 중에 돌면 엉뚱한 궤적이 됩니다. **전 성분 0** 으로 고쳤습니다.
> 시뮬레이터가 spin 조향 자세를 모델링하지 않아 **실차에서만 드러났을 버그**입니다.

### 6.3 속도·슬루율을 비를 유지한 채 함께 낮춤 🔴

`S`(실제 슬루율)를 모르는 채로 실차에 나가기 위한 조치입니다.

```
코너 진입 거리 = Δδ / (max_steer_rate / max_linear_x)

45 deg/s / 0.45 m/s = 100 deg/m → 0°→30° 진입 0.300 m
20 deg/s / 0.20 m/s = 100 deg/m → 0°→30° 진입 0.300 m   (동일)
```

비를 유지하면 **경로 모양은 그대로**이고, 슬루율 절대값이 낮아져 `명령 < 실제` 가
사실상 보장됩니다. 명령이 실제를 넘었을 때의 누적 헤딩 손실:

| 실제 S | vx=0.45(rate 45) | **vx=0.20(rate 20)** |
|---|---|---|
| 10 deg/s | 16.97° | 4.85° |
| 15 deg/s | 9.70° | 1.62° |
| **20 deg/s** | 6.06° | **0.00°** |

함께 조정한 것: MPPI 지평선 `63×0.10`(예측거리 1.26 m 복원), `vx_std 0.09`,
`min_linear_x -0.07`(전후진 비 유지 → `reverse_penalty 3.0` 근거 보존).

### 6.4 정지를 2등급으로 분리 🟠

`e-stop`/`MCU fault` 는 위험 이벤트 → **즉시 정지**(mode 0).
`cmd timeout`/`odom stale` 은 통신·센서 이슈 → **감속 램프**.

예전엔 둘을 묶어 Nav2 가 0.5 s 끊기면 0.20 m/s 에서 즉시 `mode 0` 였고,
`cmd_vel` 은 램프를 그리는데 `speed_rpm` 은 0 이라 두 필드가 어긋났습니다.

### 6.5 재계획을 사건 구동으로 🟠

**문제**: `RateController` 가 `ComputePathToPose` 를 통째로 감싸 재계획이 **순수 시간
구동**이었습니다. 경로 위에 사람이 들어와도 다음 틱까지 기다렸습니다.

**조치**: `IsPathValid` + `PathExpiringTimer` 로 "목표 변경 / 경로 무효 / 주기 도래"
셋 중 하나면 재계획.

> ⚠ 여기서 `RateController + AlwaysFailure` 를 쓰면 **동작하지 않습니다.**
> `ReactiveFallback` 이 둘째 자식으로 성공하면 첫 자식을 halt 하는데,
> `RateController::halt()` 가 내부 타이머를 리셋합니다. 실측: 1 Hz 를 의도했는데
> **18.4 Hz** 로 돌았습니다. `PathExpiringTimer` 는 `{path}` 가 바뀔 때 리셋하므로
> 이 문제가 없고, 의미상으로도 '마지막 계획 이후 N 초' 가 맞습니다.

| | 수정 전 | 수정 후 |
|---|---|---|
| 재계획 사이클 | 18.38 Hz | **0.98 → 3 Hz**(설정값) |
| `BT tick rate exceeded` | 1064 회 | **0** |
| `follow_path Aborting` | 6120 회 | **0** |

### 6.6 동적 장애물 반응 지연 단축 🟠

MPPI 지평선(1.26 m) 안에서 R_min 으로 낼 수 있는 횡변위는 0.24 m 뿐이라 차체 반폭
(0.53 m)을 못 비킵니다. **회피의 주체는 로컬이 아니라 글로벌**이고, 그 재계획이
전역 costmap 을 봅니다.

| 단계 | 이전 | 이후 |
|---|---|---|
| global costmap | 1.00 s | **0.33 s** |
| 재계획 트리거 | 1.00 s 대기 | **즉시**(`IsPathValid`) |
| `max_planning_time` | 2.00 s | **1.00 s** + `downsample_costmap` |
| **합계(최악)** | **4.30 s / 1.94 m** | **≈1.44 s / 0.65 m** |

### 6.7 도구·정합성

| | |
|---|---|
| `path_align.py` 신규 | 헤딩오차 계산 + ALIGN 기동 상태머신. ROS 비의존, 자체 시험 19항목 — §6.8 |
| `steering_observer` 신규 | `/Odometry.pose` 만으로 `κ = Δθ/Δs` → 실효 조향각. **곡률은 시간이 아니라 거리로 정의**되므로 twist=0 이어도 동작. 로깅 전용 |
| ~~`goal_yaw_aligner` 신규~~ | 도착 후 자세정렬용이었으나 **삭제**했습니다 — 어떤 런치도 안 띄웠고 ALIGN 이 대체합니다 (§6.10-D) |
| `fake_mcu` 조향 모델 | 슬루+지연, **데드레커닝을 실제 조향각으로** 계산 → 조향이 못 따라가면 진짜로 벗어남 |
| `nav2_kinematic_check` 확장 | §5 조향응답 · §6 URDF↔CONS · lattice 대조 추가 |
| URDF ↔ CONS 해소 | 휠베이스/윤거가 **뒤바뀌어** 적혀 있었음 → CONS 기준 수정 |
| `expect_mcu_state` | 업링크 미구현이 전제임을 명시. INFO 1회만 |
| 죽은 설정 정리 | `max_angular_z` 0.8→0.45, `amcl.tf_broadcast`→false |

---

### 6.8 ALIGN — 제자리 회전을 **계획단이 아니라 제어단이** 담당 🔴

이번 작업에서 **관점이 한 번 바뀐 지점**입니다. 앞선 §8.5(Lattice)와 §8.6(spin 문턱)은
둘 다 "전역 플래너가 제자리 회전을 표현하게 만들자"는 방향이었고 **둘 다 되돌렸습니다.**
`ALIGN` 은 반대 방향입니다 — 플래너는 그대로 두고, **제어단이 경로를 직접 보고
헤딩을 고칩니다.**

#### 무엇이 문제였나 — 코드가 하는 일과 의도가 달랐다

`spin` 진입 조건은 원래 이랬습니다 (`command_manager._select_auto`):

```python
spin_entry_cond = abs(wz) >= 0.35 and hypot(vx, vy) <= 0.04
```

이건 **Nav2 가 낸 twist 의 모양을 사후 분류**할 뿐입니다. "제자리 회전처럼 생긴
명령이 왔네" 하고 모드를 바꾸는 것이지, **스스로 "헤딩을 고쳐야겠다"고 판단하지
않습니다.** 그런데 전역 경로에 제자리 회전이 없으니 Nav2 가 그런 명령을 낼 이유가
없고, 결국 `spin` 은 막혔을 때 **BT 리커버리로만** 발동해 왔습니다.

`command_manager` 는 `/plan` 을 구독조차 하지 않았습니다 — 헤딩오차를 계산할 입력이
아예 없었습니다.

#### 무엇을 만들었나

```
[NORMAL] 정상 주행
   │  /plan 의 lookahead(1.0 m) 지점 pose.yaw 대비 오차 > 60° 가 0.6 s 지속
   ▼
[ALIGN]  ★ 진입 순간 목표 헤딩을 **절대각으로 래치** (매 틱 재계산 금지)
   │  vx = vy = 0,  spin 모드,  wz = clamp(1.2·남은오차, 0.15, 0.45)
   │  남은오차 < 15° 또는 타임아웃(25 s)
   ▼
[COOLDOWN] 3 s 재진입 금지 → NORMAL
```

#### 왜 이번엔 왕복하지 않는가

예전에 "요구 선회반경 < R_min 이면 spin" 이라는 **모드 분류**를 넣었다가 1틱(0.02 s)
단위 왕복으로 되돌린 적이 있습니다(`_select_auto` 설계메모). 차이는 셋입니다:

| | 되돌린 시도 | ALIGN |
|---|---|---|
| 트리거 | 요구 선회반경 (속도명령 파생) | **경로 헤딩오차** |
| 자기부정 | spin→vx=0→조건 거짓→복귀→조건 참 (진동) | spin 이 되어도 **헤딩오차는 그대로** |
| 목표 | 없음 (매 틱 재분류) | **진입 시 래치** — 기동에 끝이 있다 |
| 종료 | 조건이 사라지면 (즉시) | 목표 도달 + 히스테리시스 + 쿨다운 |

자체 시험(`python3 path_align.py`, 19항목)에 **"경로오차가 0 으로 보고돼도 기동을
유지한다"** 는 항목이 있는 이유가 이겁니다.

#### 헤딩오차를 '경로 pose 의 yaw' 로 정의한 이유 — 실측으로 갈렸다

두 정의를 동시에 재서 비교했습니다 (alm_lab 264 s, 5274 표본):

| 정의 | p50 | p90 | p99 | 최대 |
|---|---|---|---|---|
| **경로 pose 의 yaw** (채택) | 8.8° | 39.8° | 67.2° | 129.9° |
| 로봇→lookahead 방위각 (pure-pursuit 식) | 7.4° | **173.9°** | 174.0° | 174.0° |

방위각 정의는 p90 이 173.9° 입니다 — **후진(Reeds-Shepp) 구간과 목표 근방에서
통째로 뒤집힙니다.** Hybrid-A* 는 후진 구간에서도 `pose.yaw` 에 '차체 헤딩'을 넣기
때문에, pose.yaw 를 쓰면 후진을 헤딩오차 180° 로 오인하지 않습니다.

> `/plan` 이 orientation 을 실제로 채우는지도 확인했습니다 — 전 구간 `True`.
> 안 채우는 플래너로 바뀌면 `PathHeadingError` 가 세그먼트 방위각으로 대체하고
> WARN 을 한 번 띄웁니다.

#### 왜 `/plan_smoothed` 가 아니라 `/plan` 인가

토픽 주인이 다릅니다: `/plan` 은 `planner_server`(평활화 **전**), `/plan_smoothed` 는
`smoother_server`, MPPI 가 실제로 따라가는 것은 BT 블랙보드의 `{path}`(평활화 후)입니다.

`/plan` 을 고른 이유 둘:

1. **평활화가 실패해도 항상 옵니다.** BT 는 `SmoothPath` 실패를 `Fallback` 으로
   흡수하고 평활화 전 경로로 주행합니다. 그때 `/plan_smoothed` 는 낡은 채로 남습니다.
   `align_plan_stale_sec` 는 "지금 주행 중인가"의 판정도 겸하므로 이게 중요합니다.
2. **차이가 작습니다.** 두 토픽의 같은 인덱스 pose yaw 차이를 60 s 측정한 결과
   (경로쌍 117개): **p50 2.77° · p90 4.31° · 최대 9.68°**. 진입 문턱 60° 에 비하면
   무시할 수준입니다.

이탈 판정은 애초에 경로가 아니라 **래치된 절대 목표각** 대비이므로 이 차이의 영향을
받지 않습니다. 바꾸고 싶으면 `align_plan_topic` 파라미터로 갈아끼우면 됩니다.

#### 부수 효과: dwell 회계

`ALIGN` 이 `effective` 를 `spin` 으로 덮으면 기존 `mode_switch_dwell_sec`(4 s)이
자동으로 걸려 조향축(±30° → 47°)이 자리 잡을 시간을 법니다. 대신 **게이트 판정을
ALIGN 보다 먼저** 계산해서, dwell 동안에는 ALIGN 타임아웃 시계가 흐르지 않게 했습니다
— 안 그러면 dwell 이 타임아웃을 잡아먹어 **한 번도 못 돌고 끝납니다.**

> 이 작업 중에 **`mode_switch_dwell_sec` 자체가 미달이었다는 것도 드러났습니다.**
> 최악 스윕(normal 풀락 30° → spin 47° = 77°)에 3.85 s 가 필요한데 3.0 s 였습니다.
> `ALIGN` 이 이 전환을 더 자주 만들므로 **4.0 s 로 올렸습니다** (§5.3, §9.3-L).
> `nav2_kinematic_check.py` §5 가 앞으로 이 관계를 검사합니다.

#### 바뀐 파일

| 파일 | 내용 |
|---|---|
| `alm_base_control/scripts/path_align.py` | **신규.** `PathHeadingError` + `AlignManeuver`. ROS 비의존, 자체 시험 19항목 |
| `alm_base_control/scripts/command_manager.py` | `/plan` 구독 · TF(map→base_link) · `_update_align()` · 틱 순서 재배치 |
| `alm_base_control/config/base_control.yaml` | `align_*` 12개 (§5.4b) |
| `alm_navigation/scripts/nav2_kinematic_check.py` | §7 ALIGN 정합성 (히스테리시스 · 180° 회전 가능 여부 · dwell 관계) |

> **로봇 자세는 TF(`map`→`base_link`)로 읽습니다.** `/Odometry` 는 `odom` 프레임이라
> `map` 프레임인 `/plan` 과 직접 비교하면 `map→odom` 오프셋만큼 통째로 틀어집니다.
> (같은 실수를 테스트 하네스에서 이미 한 번 했습니다 — §8.7)

---

### 6.9 코드 리뷰로 잡은 결함 5건 🔴

외부 리뷰에서 6건이 제기됐고, **코드로 전부 재현해 5건을 고쳤습니다.** 1건은
실물 관측으로 오탐임이 확인됐습니다.

#### 🔴 (a) 후진 선회의 조향 부호가 반대 — 기존 결함

```python
steer_deg = -math.copysign(math.degrees(d), wz)      # wz 부호만 봤다
```

요레이트는 `ω = v / R` 이고 `R` 의 부호는 조향각이 정합니다. **같은 조향각에서
후진하면 요레이트가 뒤집힙니다.** 물리로 검산한 결과:

| 명령 | 보낸 조향각 | 실제 요레이트 |
|---|---|---|
| `vx=+0.15, wz=+0.05` | −14.78° | **+0.0500** ✅ |
| `vx=−0.15, wz=+0.05` | −14.78° | **−0.0500** ❌ 반대 |
| `vx=−0.15, wz=−0.05` | +14.78° | **+0.0500** ❌ 반대 |

Hybrid-A* 는 `reverse_penalty: 3.0` 으로 **후진을 실제로 씁니다.** 후진 선회가
반대로 돌면 MPPI 가 반대 명령을 내고 그것도 반대로 돌아 **발산**합니다.

**왜 여태 안 잡혔나** — `wz_from_steer()`(제가 만든 역함수)도 **같은 버그**였습니다.
그래서 "정합오차 2.6e-09 rad/s" 라는 검증 수치는 **둘이 똑같이 틀렸다는 뜻**이었지
맞다는 뜻이 아니었습니다. 자체 시험이 '인코딩 → 역산' 구조라 같은 규약끼리만
대조해 통과시켰습니다.

**고친 것**: `steer_deg = -copysign(deg(d), wz * copysign(1, vx))`, `wz_from_steer` 동일.
그리고 **코드를 전혀 참조하지 않고 자전거 모델로만 기대 부호를 세우는**
`[sign]` 시험을 추가했습니다. `fake_mcu` 는 `wz_from_steer` 를 쓰므로 시뮬 물리도
같이 맞습니다.

#### 🔴 (b) 조향각이 clamp 되면 전진속도가 부풀려짐 — (a) 수정 중 발견

리뷰에 없던 것입니다. `_encode_normal` 이 내측 전륜 속도를 **`wz` 기준**으로
계산했습니다:

```python
v_inner = abs(wz) * dist_inner(d)        # 낼 수 없는 wz 로 계산
```

요청 반경이 `R_min` 보다 작아 조향이 clamp 되면, **못 도는 것을 '더 빨리 가는' 것으로
보상**합니다. 실측: `vx=0.10, wz=0.10`(R=1.0 m) 요청 시 rpm 12.67 vs 정답 7.71 —
**1.64배 빠르게 달립니다.**

**고친 것**: `v_inner = |vx| · dist_inner(d) / icr_y(d)`. 비클램프 구간에서는
`icr_y(d) == |vx|/|wz|` 이라 **완전히 동일**하고, 클램프 구간만 달라집니다.

#### 🔴 (c) 직선 후진 경로를 180° 오정렬로 판단 — ALIGN 의 새 결함

`path_align` 이 "yaw 가 안 변하면 플래너가 안 채운 것" 으로 보고 세그먼트 방위각으로
대체했습니다. **그 판정이 틀렸습니다** — 직선 경로는 전진이든 후진이든 yaw 가
상수입니다. 정상적인 **직선 후진**(yaw=0, 위치는 −x 로 진행)을 미채움으로 오판하고
방위각(180°)으로 갈아끼워, ALIGN 이 **불필요한 180° 제자리 회전을 명령**했습니다.
재현값 정확히 `+180.0°`.

**고친 것**: 판정 기준을 *"yaw 가 변하는가"* → **"yaw 가 진행 방향과 정합하는가"** 로
바꿨습니다. Hybrid-A* 의 `pose.yaw` 는 차체 헤딩이므로 각 점에서
`yaw ≈ 방위각`(전진) 또는 `yaw ≈ 방위각+180°`(후진) 중 하나에 가깝습니다.
둘 다에서 멀면 미채움입니다.

그리고 미채움일 때 **추측하지 않습니다** — `evaluate()` 가 `None` 을 반환해 ALIGN 이
쉽니다. 틀린 추측의 대가(불필요한 180° 회전)가 너무 비쌉니다. 경로 추종은 MPPI 가
계속 합니다.

#### 🟡 (d) 슬루 제한 후 `speed_rpm` 이 재계산되지 않음

`_tick` 이 **인코딩 → 슬루** 순서인데, 슬루는 `steer_deg` 와 `wz` 만 되돌리고
`speed_rpm` 은 **목표 조향각 기준 그대로**였습니다. STM32 는 받은 rpm 을 '내측 전륜'
값으로 보고 나머지를 자기 조향각으로 스케일하므로 둘이 다른 선회를 가리킵니다.

```
목표 vx=0.10 wz=0.06 -> steer −29.48°, speed_rpm 7.71
  슬루로 실제 1.00° 일 때 차체속도 0.0842 m/s  (−15.8 %)
  슬루로 실제 6.00° 일 때 차체속도 0.0887 m/s  (−11.3 %)
```

**고친 것**: `rpm_for_steer(실제조향각, vx)` 를 추가하고 슬루 뒤 재계산.

#### 🟡 (e) MPPI `model_dt` 와 제어주기 불일치 — 로그에 계속 찍히고 있었음

```
[controller_server] [WARN]: Controller period is less then model dt, consider setting it equal
```

nav2 MPPI 는 **제어주기 == `model_dt`** 일 때만 control sequence shifting(warm-start)을
켭니다. 우리는 `0.05 < 0.10` 이라 **꺼져 있었습니다.**

맞추는 방법이 둘인데 `controller_frequency: 20 → 10` 을 골랐습니다 —
지평선(1.26 m)을 그대로 두면서 CPU 는 오히려 절반입니다. 대안인
`model_dt 0.05 + time_steps 126` 은 CPU 가 2.25배입니다.

수정 후: `Controller period is equal to model dt. Control sequence shifting is ON`

> **제 실수**: 시뮬 로그를 `error|abort` 위주로 걸러 봐서 이 WARN 을 놓쳤습니다.
> §9.3-F 에 "검증 통과했으므로 유지"라고 써뒀던 근거는 약했습니다.

#### 🟢 (f) `expected_planner_frequency` 가 재계획 주기와 불일치

`1.0` 인데 BT 는 3 Hz 로 재계획합니다. 계획 동작이 아니라 **경고 문턱**만 정하므로,
3 Hz 를 못 맞춰도 경고가 안 떴습니다.

`3.0` 으로 올려 실측했더니 달성률이 **2.92~2.96 Hz** 라 정상인데도 20회 중 6회
경고가 떴습니다(타이머 지터). 문턱을 명목치에 붙이면 진짜 경고가 묻히므로
**2.5** (약 17% 여유)로 정했습니다.

#### ✅ (g) 오탐 — 모드 전환 dwell 이 조향을 준비 못 한다

리뷰는 "dwell 중 `steer_deg=0` 이라 STM32 가 미리 조준 못 한다" 고 했고, 저도 처음엔
동의했습니다. **실물 관측으로 뒤집혔습니다**:

> 모드를 바꾸면 STM32 가 알아서 바퀴를 맞추고, 그 뒤 쓰로틀을 올리면 돈다

`uart_protocol.md` 와도 맞습니다 — 크랩/제로턴은 `steer_deg` 의 **크기를 무시**하고
각도는 STM32 가 `CONS(8)/CONS(9)` 로 갖고 있습니다. 즉 `mode_id` 만으로 조준이
시작됩니다. dwell 은 **의도대로 동작하고 있었습니다.**

> 다만 확인된 사실 하나: 모드 전환 순간 `vx` 는 가속제한으로 램프 하강하므로
> **약 0.2 s(2 cm) 동안은 조향축이 도는 중에 굴러갑니다.** 현재 속도에서는
> 무시할 수준이지만 속도를 올리면 비례해 커집니다 — §9.3-M.

---

### 6.10 워크스페이스 전수 점검 — 센서·배선·리커버리 🔴

제어/계획 밖까지 훑었습니다. **초기 측위는 동작 확인됨**을 전제로, 그 뒤 주행에서
문제가 될 것을 찾았습니다.

#### 🔴 (A) LiDAR 마운트 TF 가 추정값 — **미해결, 줄자 필요**

`alm_sensors/launch/lidar.launch.py`:

```python
DeclareLaunchArgument("lidar_x", default_value="0.0")   # ##TODO## 실측 마운트
DeclareLaunchArgument("lidar_y", default_value="0.0")
DeclareLaunchArgument("lidar_z", default_value="0.5")
"--yaw", "0", "--pitch", "0", "--roll", "0",
```

이 한 값이 **세 곳에 동시에** 들어갑니다:

| 영향 | 결과 |
|---|---|
| FAST-LIO `odom→base_link` | 보고하는 위치가 **실제 차체와 오프셋만큼 어긋남** |
| costmap 장애물 배치 | 벽이 실제보다 앞/뒤로 밀려 찍힘 |
| `/scan` z밴드(±0.25 m) | 엉뚱한 높이를 봄 |

> **"초기위치가 잘 잡힌다"로는 안 잡힙니다.** 맵도 같은 TF 로 만들었다면 매핑과
> 측위가 **일관되게 같이 틀려서** ICP 는 잘 수렴합니다. 어긋나는 것은
> **맵과 실제 차체의 관계**이고, 그건 footprint 충돌검사에 그대로 들어갑니다.
> 1.37 m 차체가 0.53 m 여유 통로를 다니는데 0.3 m 어긋나면 사고입니다.

roll/pitch 도 확인하세요 — MID-360 은 기울여 다는 경우가 많습니다.

#### 🔴 (B) 후방 1.5 m 사각지대 — 경고 추가

```
mask_enable: true, mask_center_deg: 180, mask_width_deg: 60, mask_max_range: 1.5
```

적재물 자기가림 대책인데, `/livox/lidar` **자체를** 깎으므로 FAST-LIO · ICP ·
Nav2 costmap · `/scan` 에 **동시에** 적용됩니다. 즉 그 영역은 "적재물이 안 보이는"
게 아니라 **아무것도 안 보이는** 영역입니다.

그리고 이 로봇은 **후진을 실제로 씁니다**:
`reverse_penalty: 3.0`(금지가 아님) · BackUp 리커버리 0.30 m · `min_linear_x: -0.07`.
`local_costmap` 에는 `static_layer` 가 없어서 뒤쪽은 전역 costmap 의 정적 맵으로만
압니다 — **맵에 없는 물체로 후진하면 못 봅니다.**

**값은 안 바꿨습니다.** 줄이면 적재물 점이 FAST-LIO 정합에 섞여 **측위가 흔들립니다**
(지금 잘 되는 것을 깨뜨립니다). 대신 기동 시 경고를 띄웁니다:

```
★ 후방 사각지대 1.50 m (방위 180±30deg).
  이 영역의 장애물은 **어떤 센서에도 잡히지 않습니다.**
  마스크는 적재물을 정확히 덮는 최소값으로 줄이세요.
  튜닝: lidar.launch.py mask_debug:=/livox/lidar_masked
```

> ⚠ 한 가지 완화가 있습니다: 마스크 영역은 점이 아예 없으므로 **레이캐스팅도 안 일어나
> clearing 이 되지 않습니다.** 즉 뒤로 가기 전에 이미 본 장애물은 local costmap 에
> **남아 있습니다.** 못 보는 것은 '내가 등진 뒤에 새로 나타난 물체' 입니다.

#### 🟡 (C) `local_costmap.update_frequency` 5 → 10 Hz

MPPI 는 20 Hz 로 1600개 궤적을 평가하는데 장애물 지도는 5 Hz 였습니다 —
**최대 0.2 s 낡은 코스트맵**. 0.20 m/s 에서 4 cm 라 정적 환경에선 무시할 만하지만
동적 장애물에는 그대로 지연입니다. 8×8 m @0.05 = 160×160 셀이라 2배도 부담이 작습니다.

#### 🟡 (D) `goal_yaw_aligner` 제거

도착 후 자세를 `/spin` 액션으로 맞추던 노드입니다. **어떤 런치도 띄우지 않고 있었고**
(설치만 돼 있었음), 실측에서 한 번도 발동하지 않았으며, ALIGN(§6.8)이 같은 일을
더 앞에서 액션 선점 없이 합니다. **회전을 명령하는 주체가 둘인 구조 자체가 위험**해
삭제했습니다.

#### ✅ 점검해서 문제 없던 것

실행 중인 스택의 **토픽 그래프로 직접 확인**한 것들입니다:

| 항목 | 결과 |
|---|---|
| 명령 사슬 | `controller_server → /cmd_vel_nav → velocity_smoother → /cmd_vel → cmd_arbiter → /cmd_vel_mux → command_manager → /mcu/command` ✅ |
| 리커버리 한계 | `behavior_server.max_rotational_vel 0.45` = 우리 spin 상한, `backup_speed 0.05` < `\|min_linear_x\| 0.07` ✅ |
| BT 두 종류 | `navigate_to_pose` / `navigate_through_poses` 둘 다 `PathExpiringTimer` + `IsPathValid` ✅ |
| `mcu_bridge` | `SerialException` 처리 + 1 s 주기 재접속 ✅ |
| `auto_crab_enabled` | `false` — dwell 6 s 필요한 crab 이 자율주행에서 안 걸림 ✅ |
| local costmap 크기 | 8×8 m rolling ≫ MPPI 지평선 1.26 m + footprint ✅ |
| `drive_mode` 미발행 | `cmd_arbiter._publish_mode` 가 `None` 을 걸러 `command_manager` 가 기본 `auto` 유지 ✅ |
| FAST-LIO 프레임 | `base_frame_id: base_link` 설정됨 (TODO §4 의 "odom→sensor" 우려는 해소) ✅ |

#### ⚠ 알고는 있으나 안 고친 것

**`/Odometry.twist` 가 항상 0** 입니다(FAST-LIO 가 pose 만 채움). MPPI 는 매 주기
**"나는 정지 상태" 라고 믿고** 롤아웃을 시작합니다. 고치려면 pose 를 수치미분해야
하는데, 10 Hz LiDAR pose 를 미분하면 잡음이 커서 득실이 불분명합니다.

> **다만 이건 시뮬이 그대로 재현하고 있습니다** — `sim_world` 가 twist 를 일부러
> 비워 둡니다. 즉 지금까지의 모든 측정 결과에 이 조건이 **이미 포함**돼 있습니다.

---

### 6.11 출발 헤딩 — 경로는 항상 내 헤딩에 접해서 나온다 🔴

#### 증상

벽을 보고 세워 둔 로봇 **뒤쪽**에 목표를 찍으면, 전역경로가 출발하자마자 옆으로
크게 튀어 R_min 으로 감아 돌았습니다. "경로가 이상하다" 로 보였지만 **경로는
정상**이었습니다.

#### 원인 — 두 겹이었다

**(1) 경로가 튀는 것은 정상이다.**
Hybrid-A* 는 탐색의 시작 상태 θ 를 **현재 로봇 헤딩**으로 두고 R_min 원호만 이어
붙입니다(§2 ①). 벽을 보고 있으면 "벽 쪽으로 나가서 크게 감아 돌아라" 외의 해가
없습니다. 고쳐야 할 것은 경로가 아니라 **출발 헤딩**입니다.

**(2) 그런데 헤딩을 고치는 장치(ALIGN)가 출발 시점에 작동하지 않았다.**
ALIGN 은 '경로가 요구하는 헤딩' 과 내 헤딩을 비교하는데, 경로가 내 헤딩에 **접해서**
출발하므로 그 오차가 호각으로 묶입니다:

```
|경로 헤딩오차|_max = lookahead / R_min = 1.0 / 1.643 = 34.9°
                     진입 문턱 align_enter_deg = 60.0°
```

즉 **출발 시점에는 수학적으로 절대 안 걸렸습니다.** 값이 멀쩡해 보여서 오래 안
보였습니다. `nav2_kinematic_check.py` 가 이제 이 관계를 검사합니다.

**(3) '정지' 를 속도로 정의한 것도 틀렸다.**
처음 고칠 때 `|vx| < 0.05` 를 '정지' 로 뒀는데, 통합 시험에서 **한 번도 안 걸렸습니다.**
목표를 받으면 Nav2 가 즉시 `cmd_vel` 을 내고 가속 1.0 m/s² 라 **0.15 s 만에** vx=0.15
가 됩니다. 그런데 진입 지속시간이 바로 그 0.15 s 라, 지속시간이 차기 전에 조건이
무너졌습니다. **이동거리(0.30 m)로 정의**해야 2.0 s 의 실효 창이 생깁니다.

#### 무엇을 만들었나 — 세 겹

| | 무엇 | 왜 |
|---|---|---|
| ① | **출발 구간 전용 lookahead** `align_lookahead_m_stopped: 3.0` | 오차 상한을 34.9° -> 104.6° 로 연다. 주행 중 값은 그대로 1.0 (실측 분포가 그 기준) |
| ② | **출발 전 목표 방위각 pre-align** | 경로 대신 **목표까지의 직선 방위각**을 오차로 쓴다. ①로도 '접선 출발' 이라는 사실 자체는 못 바꾸므로, 이것만이 180° 를 즉시 본다 |
| ③ | **출발 구간 목표 재래치** | 한 기동이 도는 각도가 '래치각 − 이탈각' 으로 묶이던 것을 푼다. 왕복 1회 비용이 dwell 5 s × 2 라 이게 시간예산을 통째로 먹었다 |

②의 안전장치: **목표당 1회, 아직 0.30 m 미만 이동한 동안만.** 주행 중에 다시 서더라도
쓰지 않습니다 — 코너를 도는 중이면 목표 방위각이 경로와 다른 게 정상이고, 거기서
방위각을 쫓으면 벽으로 향합니다. 목표 좌표는 `/plan` **끝점**에서 뽑습니다
(`/goal_pose` 를 안 쓰는 이유: 목표가 NavigateToPose 액션으로 직접 올 수도 있습니다).

③의 폭주 방지 두 겹: **회전 방향을 절대 안 바꾸고**(부호가 뒤집히면 재래치 중단 =
단조 회전 보장), **늘리기만 합니다**(낡은 `/plan` 이 기동을 조기 종료시키지 못하게).
상한은 `align_max_sec` 이 잡습니다(0.25 rad/s × 25 s = 358°, 약 1회전).

#### 검증 — ROS 스텁 통합 시험

`command_manager._tick` 을 그대로 돌리고 가짜 Nav2(Dubins 유사 경로 재계획 2.5 Hz +
Ackermann 한계 경로추종)를 붙였습니다. 8 m 떨어진 목표, vx=0.15 m/s.

| 목표 방향 | 이전 (spin 횟수 / 도달시간) | 현재 | |
|---|---|---|---|
| 정면 (오차 0°) | 0회 / 50.1 s | 0회 / **50.1 s** | 회귀 없음 |
| 비스듬 (30°) | 1회 / 61.6 s | **0회** / **50.5 s** | 불필요한 spin 제거 |
| 옆 (90°) | 4회 / 97.0 s | **1회** / **65.8 s** | |
| 뒤 (180°) | **8회** / 145.7 s | **1회** / **72.3 s** | 증상 그 자체 |

'뒤 180°' 의 이전 거동이 정확히 예측대로였습니다 — **22° 씩 8번 나눠 돌면서** 왕복마다
dwell 을 두 번씩 냈습니다. 직선거리 8 m 를 0.15 m/s 로 가면 53 s 이므로, 현재의 72.3 s
는 회전 17 s + dwell 10 s 를 더한 거의 최적값입니다.

'비스듬 30°' 가 개선된 것은 덤입니다. 예전 `align_enter_deg_stopped: 25°` 가 정상적인
S자 출발까지 물어 불필요한 spin 을 냈고, 60° 로 올리면서 사라졌습니다.

> ⚠ 시뮬레이션입니다. 가짜 Nav2 는 MPPI 가 아니고, 조향 슬루율은 여전히 미실측입니다
> (§4). 실차에서는 `run_recorder` 의 `mode.spin_segments` 로 **한 번에 도는지**를
> 먼저 보세요 — 구간이 4개 이상 나오면 위 ③이 안 먹고 있는 것입니다.

#### 바뀐 파일

| 파일 | 내용 |
|---|---|
| `alm_base_control/scripts/path_align.py` | `evaluate(lookahead_m=)` 오버라이드, `endpoint()`, 재래치, 진입 문턱 오버라이드. 자체 시험 +14항목 |
| `alm_base_control/scripts/command_manager.py` | 출발 구간(이동거리) 판정, pre-align, `/plan` 끝점 기반 새 목표 감지, 기동 시 상한 대조 로그 |
| `alm_base_control/config/base_control.yaml` | `align_*` 8개 추가 · `align_enter_deg_stopped` 25 -> 60 |
| `alm_navigation/scripts/nav2_kinematic_check.py` | 출발 시 헤딩오차 상한 검사 추가 (+ `align_enabled: false` 에서 죽던 버그 수정) |

---

## 7. 검증 결과 (시뮬레이션)

### 7.1 어떻게 돌렸나

**파라미터는 하나도 바꾸지 않고** 실제 `nav2.yaml` · `base_control.yaml` · 커스텀 BT 를
그대로 썼습니다. 하드웨어만 대역했습니다.

```
[sim_world]   실차에서 FAST-LIO + LiDAR 가 하는 일
   · /wheel_odom -> /Odometry(odom 프레임), TF map->odom->base_link
   · 정적맵 레이트레이싱으로 /scan 생성 (10 Hz, 360°)
       ★ local_costmap 은 static_layer 없이 obstacle_layer 만 있어서,
         이게 없으면 MPPI 가 벽을 전혀 못 본다
   · 매 틱 footprint 충돌 검사
   · 측위는 완벽하다고 가정 -> 여기서 실패하면 측위 탓이 아니라 계획/제어 문제다

[fake_mcu]    조향 슬루 60 deg/s + 지연 0.08 s. **데드레커닝을 실제 조향각으로** 계산
              -> 조향이 못 따라가면 로봇이 진짜로 경로를 벗어난다

실제 코드 그대로: cmd_arbiter -> command_manager, nav2_bringup navigation_launch.py
맵: alm_lab (실측 SLAM, 50.7 × 33.6 m)
     + tight_L (합성, 18 × 10 m) — 복도 3.0 m 폭 + 막다른 포켓.
       ★ R_min 유턴 가능 셀 **0개**. ALIGN 을 겨냥해 만든 맵이다 (§7.3)

[heading_probe]  경로 헤딩오차 관측 전용. 제어 경로를 건드리지 않는다.
                 ALIGN 문턱을 추측이 아니라 실측 분포로 정하기 위해 먼저 돌렸다.
[run_ab.sh]      ALIGN 만 토글해 A/B. **반복 실행 필수** — §8.9
```

목표는 **경로가 실제로 좁은 곳을 지나는지**로 골랐습니다 — 측지거리(BFS)와 경로상
최소여유를 계산해서.

### 7.2 결과

**최종 구성**(GridBased 단독 · spin 문턱 0.35 · 이번 조향/BT/속도 변경 적용) 기준입니다.

| 목표 | 성격 | 결과 | 소요 | 최소여유 | 충돌 | 목표오차 |
|---|---|---|---|---|---|---|
| 개활 대조군 | 경로 최소여유 1.50 m | ✅ | 88.3 s | 0.752 m | **0** | 0.132 m / 11.4° |
| 협착 (G4) | 경로 최소여유 0.53 m | ✅ | 101~134 s (3/3) | 0.255 m | **0** | 0.234 m / 6.0° |
| 하드케이스 (G3) | 목표 근처 ~90° 자세변화 | ✅ | 174~296 s | 0.112 m | **0** | 0.23 m / 0.2~2.0° |

⚠ G3 는 이전 판에서 ❌ 였습니다. 그 뒤 여러 번 돌린 결과 **성공/실패가 실행마다
바뀝니다**(§8.9). 성공 여부 하나로 판정하지 마세요.

**추가 맵**: ALIGN 을 겨냥해 협착 ㄱ자 맵(`make_lmap.py`, R_min 유턴 가능 셀 0개)을
따로 만들어 A/B 를 돌렸습니다 — §7.3.

### 전 시험 통틀어 **충돌 0**

여러 구성으로 30회 이상 목표를 실행했고, **단 한 번도 부딪히지 않았습니다.**
실패는 전부 '목표에 못 붙음'이지 '충돌'이 아닙니다.

### 이번 수정의 실측 검증

| 항목 | 결과 |
|---|---|
| `cmd_vel.wz` ↔ `steer_deg` 정합 | 최대오차 **2.6e-09 rad/s** (n=2603) ⚠ 이 수치는 **전진 구간에서만 유효**했습니다 — 인코더와 역함수가 후진에서 같이 틀려 있었기 때문입니다(§6.9-a). 수정 후 재측정 필요 |
| 조향 슬루 제한 (설정 20 deg/s) | p99 **20.45** deg/s (초과 0.91% = 타이머 지터) |
| **명령 vs 실제 조향 간극** | 평균 **0.010°**, 최대 0.406° |
| 조향각 기구한계 (±30°) | -30.0 ~ +2.5° — 포화하되 초과 없음 |
| normal 30° 초과 가능성 | **12,321 조합 전수 스캔 → 최대 정확히 30.0000°** |
| 재계획 사이클 | 18.38 Hz(버그) → 설정값대로 |
| `BT tick exceeded` / `follow_path Aborting` | 1064 → **0** / 6120 → **0** |
| 업링크 없이 주행 | `/mcu/state` 없이 정상 (`/wheel_odom` 50 Hz) |

### 7.3 ALIGN 검증 — 무엇이 확인됐고 무엇이 확인 안 됐나

솔직하게 나눠 적습니다. **ALIGN 은 설계대로 동작하지만, §7.3 의 원래 실패를 고치지는
못했습니다.** 그 실패의 원인이 헤딩이 아니었기 때문입니다.

#### ✅ 확인된 것 1 — 발동해야 할 때 발동하고, 설계대로 끝난다

alm_lab 3목표 640 s 주행 중 **1회** 발동:

```
ALIGN 진입: 경로 헤딩오차 -62.4° -> 목표 헤딩 +30.0° 로 제자리 회전
ALIGN 종료(정렬 완료): 남은 오차 -14.9° -> normal 복귀        (5.2 s 뒤)
```

진입→래치→회전→이탈→복귀가 한 번에 끝났습니다. **왕복(진동)은 없었습니다** —
되돌렸던 반경기반 라우팅의 실패 모드가 재현되지 않았습니다.

#### ✅ 확인된 것 2 — 발동하면 안 될 때는 완전한 무동작

`align_enabled` 만 바꾼 A/B 를 개활맵 2회 · 협착맵 1회 돌렸습니다.
ALIGN 이 걸리지 않는 목표에서는 숫자가 **소수점까지 일치**합니다:

| 목표 | ALIGN 없음 | ALIGN 있음 |
|---|---|---|
| 개활 대조군 (alm_lab F1) | 87.1 s / 0.138 m / 11.4° | 87.2 s / 0.137 m / 11.4° |
| 협착 복도 직진 (L0) | 71.8 s / 0.240 m / 5.3° | 70.5 s / 0.239 m / 5.0° |
| 협착 복도 복귀 (L2) | 62.1 s / 0.158 m / 4.9° | 62.1 s / 0.159 m / 4.8° |
| 개활 대조군 (dwell 4.0 재확인) | 87.3 s / 0.133 m / 11.4° | 87.1 s / 0.140 m / 11.4° |

**회귀 없음.** 켜 두는 비용이 0 입니다.

> 같은 실행의 협착 목표들은 좌우가 크게 갈렸지만(F2 163 s ↔ TIMEOUT, F3 실패 ↔ 성공),
> **그 실행에서 ALIGN 은 0회 발동했습니다.** 즉 그 차이는 ALIGN 이 아니라 하네스
> 잡음입니다 — §8.9. 위 표에 개활/직진 목표만 올린 이유입니다.

#### ✅ 확인된 것 3 — 문턱 60° 는 실측 분포에 맞다

`heading_probe.py` 로 두 맵에서 잰 \|경로 헤딩오차\|:

| 상황 | p50 | p90 | max | >60° 비율 |
|---|---|---|---|---|
| alm_lab, 전 구간 | 8.8° | 39.8° | 129.9° | 1.2% |
| 협착맵, **경로 추종 중**(v>0.03) | 5.7° | **9.7°** | 96.2° | 4.6% |
| 협착맵, **정지 중**(경로 없음/낡음) | 96.2° | 156.8° | 179.9° | 69.1% |

**경로를 따라가고 있을 때는 헤딩오차가 거의 안 벌어집니다** (p90 9.7°).
큰 오차는 전부 '경로가 없어 멈춰 있는' 구간입니다. 즉 ALIGN 은 원래 드물게
개입하는 것이 맞고, 문턱을 낮추면 정상 주행을 망칠 뿐입니다.

> ⚠ 처음에 협착맵 전체 통계가 ">60° 가 22~28%" 로 나와 ALIGN 이 왜 안 걸리나
> 의심했습니다. 그 숫자는 **낡은 경로 때문에 부풀려진 것**이었습니다
> (`heading_probe` 는 경로 신선도를 안 봅니다). 위 표처럼 주행/정지로 나눠야
> 의미가 있습니다.

#### ❌ 확인 안 된 것 — 원래의 §7.3 실패는 ALIGN 이 못 고친다

원래 실패(목표 근처의 큰 자세변화)를 정면으로 겨냥해 **전용 맵**을 만들었습니다:

```
협착 ㄱ자 맵 (make_lmap.py)
  주 복도 3.0 m 폭  — 로봇(1.37 x 1.06 m)은 여유롭게 다니지만
  막다른 포켓 2.8 x 2.6 m
  ★ R_min(1.643 m) 유턴이 가능한 셀 = 0 개  (설계 확인 완료)
```

목표 `L1 = 포켓 안에서 바깥을 향한 자세`. 결과는 **ALIGN 유무와 무관하게 실패**:

```
replans: 0        ← /plan 이 한 번도 발행되지 않았다
[planner_server] GridBased: failed to create plan, no valid path found.
```

**ALIGN 은 `/plan` 을 입력으로 삼습니다. 경로가 아예 없으면 볼 것이 없습니다.**

그러면 "먼저 돌고 나서 계획하면 되지 않나"가 다음 질문입니다. 실측했습니다.

**(a) Nav2 리커버리 spin 은 실제로 돌았다.** 실패 구간의 로봇 yaw 궤적:

```
t+0 s   +5°   →  t+13 s  +96°   (리커버리 spin 1회)
t+38 s  +97°  →  t+42 s  -172°  (리커버리 spin 2회)
```

**(b) 그런데 그 헤딩들에서 계획이 계속 실패했다.** 시작 헤딩만 바꿔 같은 목표를
보낸 실험:

| 시작 헤딩 | 결과 | 재계획 |
|---|---|---|
| 0° | 실패 (0.8 s 만에) | 0 |
| −45° | 실패 | 0 |
| −90° (포켓으로 후진 진입 가능한 자세) | 실패 | 0 |

> **결론: 이 목표는 헤딩과 무관하게 계획 불가입니다.** 원인은 조향 능력이 아니라
> 플래너/코스트맵 쪽입니다 — 포켓 폭 2.8 m 에 `inflation_radius` 가 1.2 m 라
> 포켓 중앙조차 팽창비용이 높고, `max_planning_time: 1.0` 안에 해를 못 찾습니다.
> **따라서 이 실험은 ALIGN 의 시험이 아니었습니다.** ALIGN 의 성패로 읽으면 안 됩니다.

#### 그래서 지금 어디까지 왔나

| | |
|---|---|
| 경로가 있고 헤딩만 어긋난 경우 | ✅ **ALIGN 이 고친다** (실측 1회, 5.2 s) |
| 경로가 있는데 정상 주행 중 | ✅ ALIGN 무동작, 회귀 없음 |
| **경로가 아예 안 나오는 경우** | ❌ **미해결.** ALIGN 은 입력이 없어 개입 불가 |

마지막 칸이 남은 일입니다. 그리고 그 원인은 **제어단이 아니라 계획단**입니다:
`inflation_radius` / `max_planning_time` / 포켓형 목표의 코스트맵 처리.
§9.3 의 항목 K 로 옮겼습니다.

> 이전 판의 §7.3 은 이 실패를 "플래너가 제자리 회전을 표현 못 해서"로 설명했습니다.
> **그 설명은 불완전했습니다.** 제자리 회전을 쓸 수 있게 만들어도(리커버리 spin 이
> 실제로 96°·187° 로 돌려놨습니다) 계획은 여전히 실패했습니다.

---

### 7.4 이 검증이 증명하지 못하는 것

솔직하게 적습니다. **실차는 미검증**이고, 아래가 전부 가정입니다.

| | |
|---|---|
| ❌ **조향 슬루율** | `fake_mcu` 에 60 deg/s 를 **제가 넣었습니다.** 실제 값은 모릅니다 (§9.1) |
| ❌ **정지 시 조향/스톨** | 시뮬은 정지 상태에서도 60 deg/s 로 돕니다. 실차는 훨씬 느리고 스톨할 수 있습니다 |
| ❌ **측위 오차** | `map→odom` 을 항등으로 고정. FAST-LIO 드리프트·ICP 재측위 점프 없음 |
| ❌ **슬립** | 구동 손실 4%만 흉내. 2축 구동 스크럽 없음 |
| ❌ **동적 장애물** | 정적 맵만. §6.6 의 지연 개선은 계산값이지 실측이 아님 |
| ❌ **`/scan` 현실성** | 완벽한 레이트레이싱. LiDAR 잡음·자기가림 없음 |
| ⚠ **CPU** | 개발 PC(8코어) 기준. **Orin Nano 는 훨씬 빡빡함** |
| ⚠ **spin/crab 조향 자세** | `fake_mcu` 가 모델링하지 않음 → §6.2 같은 버그를 시뮬이 못 잡음 |

**이 검증이 말해주는 것은 "계획-제어 사슬의 논리와 파라미터 정합성이 맞다" 이지
"실차에서 된다" 가 아닙니다.**

---

## 8. 시행착오 기록 — 틀린 가설들

작업 중 **가설을 세우고 검증으로 뒤집힌 것이 여러 번**입니다. 같은 길을 다시 가지
않도록 압축해서 남깁니다. 공통 교훈은 하나입니다 — **변수를 하나씩 분리하지 않으면
원인을 못 찾는다.**

### ❌ 8.1 `auto_mode_min_hold_sec` 를 4.0 으로 올림 → 되돌림

**가설**: "전환 비용(dwell, 당시 3 s)을 냈으면 그 모드를 최소한 쓸 수 있어야 한다."
실측 로그에서 spin 체류가 1.16 s 로 dwell 도 못 채우고 나가는 걸 봤습니다.

**결과**: 협착 목표 2개가 성공 → 타임아웃으로 **악화**.
spin 을 오래 붙들면 MPPI 가 목표 헤딩을 지나쳐도 못 빠져나옵니다 —
탈출 조건이 `|wz| ≤ 0.03` 인데 되돌리려는 명령이 계속 나오기 때문입니다.
**짧게 자주 전환하는 편이 실제로 낫습니다.** → 0.80 으로 원복.

> '6초 낭비' 자체는 실재합니다. 다만 해법은 min_hold 상향이 아니라
> (a) `mode_switch_dwell_sec` 축소, (b) spin 탈출 조건을 헤딩오차 기반으로 바꾸기입니다.

### ❌ 8.2 "재계획 주기가 원인" → 아니었음

**가설**: 1 Hz 재계획으로는 낡은 경로를 0.20 m 나 따라가므로 협착에서 실패한다.

**결과**: 3 Hz 로 올려도 **동일하게 실패**.
다만 근거("재계획 주기 ≤ costmap 갱신률")는 그 자체로 성립하고 부작용이 없어
**3 Hz 는 유지**합니다. ⚠ 하드케이스를 고치려다 못 고친 변경임을 기록해 둡니다.

### ❌ 8.3 "목표 체커가 xy AND yaw 를 함께 요구해서 교착" → 아니었음

**가설**: 체커에서 yaw 를 빼면 위치로 먼저 성공하고, 그 뒤 `goal_yaw_aligner` 가
자세를 맞출 수 있다.

**결과**: `yaw_goal_tolerance: 0.20 → 3.15`(무시)로 해도 **동일하게 실패**.
구속하는 것은 체커가 아니라 **경로**였습니다 — Hybrid 가 만든 경로의 마지막 pose 가
여전히 목표 yaw 를 담고 있고, MPPI 가 `PathAlignCritic`(가중치 14, 최대)로 그 자세를
쫓기 때문입니다. → **0.20 으로 원복** (체커만 풀면 얻는 것 없이 yaw 강제만 잃음).

### ❌ 8.4 "TightSpace 를 폴백으로" → 죽은 코드였음

**가설**: `<Fallback>` 으로 GridBased 실패 시 TightSpace 로 넘긴다.

**결과**: **GridBased 실패 0회** — TightSpace 가 한 번도 안 불렸습니다.
`Fallback` 은 첫 자식이 FAILURE 일 때만 넘어가는데, Hybrid 는 경로를 항상 **찾습니다**.
다만 그 경로가 요구하는 최종 자세를 로봇이 못 맞출 뿐입니다 —
**실패가 아니라 '따라갈 수 없는 성공'** 이라 트리거가 성립하지 않았습니다.
→ TightSpace 를 주 플래너로 바꿈 (§7 에 그 결과).

### ❌ 8.5 `TightSpace`(Lattice) 플래너 도입 → 비활성화

**가설**: `SmacPlannerHybrid` 가 제자리 회전을 표현 못 하는 게 근본 문제이므로,
회전 프리미티브를 가진 `SmacPlannerLattice` 로 풀면 된다.
맵 분석도 뒷받침했습니다 — 통행가능 영역의 **33.1%** 는 R_min 만으로 방향을
되돌릴 수 없고, 회전을 허용하면 96.4% 가 하나로 이어집니다.

**한 것**: nav2 공식 생성기로 이 플랫폼 전용 control set 을 구웠습니다
(408개 프리미티브, 제자리회전 32개, 최소 실제 반경 1.677 ≥ R_min 1.6425).
`rotation_penalty` 를 실제 비용에서 유도했습니다 — 회전 1회 ≈ dwell(당시 3 s)×2 =
normal 로 1.20 m = 격자 24셀 → 5.0.

**결과**: 개활 주행에는 회귀가 없었지만(95.7 s vs 87.9 s, +9%) **하드케이스가
오히려 크게 나빠졌습니다.**

| 구성 | 하드케이스 |
|---|---|
| GridBased 단독 | 0.237 m / 14.8° |
| TightSpace 주 (기본) | 1.255 m / 64.3° |
| TightSpace 주 (3.0 s + 휴리스틱 캐시) | 1.651 m / 38.9° |

원인은 TightSpace 가 `no valid path found` 로 자주 실패해 GridBased 와 번갈아
나오면서 **경로가 불안정해진** 것입니다. → `planner_plugins` 에서 제외(로드도 안 함).

> **산출물은 남겼습니다** — `alm_navigation/lattice_primitives/` 에 control set 과
> 생성법이 있고, 검사기도 대조합니다. 방향(넓은 곳은 빠르게, 좁은 곳은 회전)은
> 맞지만 **TightSpace 의 계획 실패율을 먼저 규명해야** 쓸 수 있습니다.

### ❌ 8.6 spin 진입 문턱 0.35 → 0.10 → 되돌림

**유도**: `spin_lin(0.04)/R_min = 0.0244` × 안전계수 4 = 0.10.
"normal 로는 못 만드는 회전"을 문턱으로 삼는다는 논리이고,
기존 0.35 는 `vx_max=0.45` 시절 값(계수 14.3)이 남은 것이었습니다.

**결과**: 순 효과 없음.

| 문턱 | G4 협착 | G3 하드 |
|---|---|---|
| **0.35** | 성공 101s/134s/127s (**3/3**) | 1/4 |
| 0.10 | **실패** (0.571 m/37°) | 0/2 (0.237 m/14.8° 로 근접했으나) |

**해석**: 0.35 에서는 spin 이 사실상 'BT 리커버리 전용'입니다(Spin 이 0.45 를 명령).
0.10 으로 낮추면 MPPI 가 우연히 내는 회전에도 걸리는데, **spin 은 위치오차를 줄이지
못하고** 탈출 조건이 `|wz| ≤ 0.03` 이라 목표 헤딩을 지나쳐도 못 빠져나옵니다
(체류 7~14.5 s 실측). **더 도는 게 더 나은 게 아닙니다.**

> ##TODO## 진짜 문제는 문턱값이 아니라 **spin 탈출 조건이 요레이트 기반**이라는
> 것입니다. "얼마나 더 돌아야 하는가"(헤딩오차)가 아니라 "지금 얼마나 빨리 도는가"만
> 보므로 오버슈트를 못 빠져나옵니다. 헤딩오차 기반으로 바꾸면 문턱을 낮춰도
> 안전해질 가능성이 있습니다.

### 🔧 8.7 하네스(테스트 코드) 버그 5건

제품이 아니라 제 검증 코드의 버그입니다. 같은 함정을 피하려고 남깁니다.

1. **PGM 행 순서** — PGM 첫 행은 y 최대, `OccupancyGrid` 는 y 최소. 안 뒤집으면
   가짜 LiDAR 가 빈 공간에 벽을 찍어(유령 장애물 16,446 셀) "출발점이 lethal" 실패.
2. **`/Odometry` 프레임 오용** — odom 프레임인데 map 좌표로 씀.
3. **기동 대기를 '노드 존재'로 판정** — lifecycle 활성화보다 2초 빨라 목표 3개가
   `GOAL_REJECTED`. `ros2 lifecycle get` 이 `active [3]` 이 될 때까지 기다려야 함.
4. **`publish_state=false` 가 시뮬레이터 위치 출력까지 차단** — `/wheel_odom` 은
   실물에선 업링크에서 오지만 **시뮬에서는 세계의 진짜 위치**라 로봇이 안 움직였음
   (`travel_m 0.0` 인데 `vx_mean 0.106`).
5. **셸 확장 오류** — `${NO_UPLINK:+false}${NO_UPLINK:-true}` → `false1` →
   `fake_mcu` 가 파라미터 타입 예외로 **죽어 있었음**.

### 🔴 8.8 시뮬에서 안 잡히는 버그도 있었음

**모드 전환 dwell 이 `spin` 에서 회전을 안 막던 문제** — `if effective == "normal"`
조건 때문이었습니다. spin 의 회전은 바퀴 구동으로 만들어지므로, 조향축이 아직 제로턴
자세(47°)로 가는 중에 돌면 의도와 전혀 다른 궤적이 됩니다.

**시뮬레이터에서는 안 잡힙니다** — `fake_mcu` 가 `normal` 에서만 실제 조향각으로
궤적을 계산하고 `spin/crab` 은 명령을 그대로 쓰기 때문입니다.
**실차에서만 드러났을 버그**입니다. ##TODO## `fake_mcu` 가 spin/crab 조향 자세도
모델링하게 만들면 다음엔 잡힙니다.

---

### ❌ 8.10 `controller_frequency` 20 → 10 → **되돌림**

**가설**: MPPI 는 제어주기 == `model_dt` 를 요구한다(§6.9-e). 맞추는 방법이 둘인데,
`controller_frequency` 를 10 Hz 로 낮추면 지평선(1.26 m)을 그대로 두면서 CPU 도
절반이 된다. 0.20 m/s 에서 한 제어주기는 2 cm 뿐이니 감수할 만하다.

**결과**: **명백히 나빠졌습니다.** 같은 3목표에서

| | 20 Hz (model_dt 0.10, 불일치) | **10 Hz** | 20 Hz (model_dt 0.05×126) |
|---|---|---|---|
| `BT tick rate exceeded` | 0 | **815** | 0 |
| `Optimizer fail to compute path` | 0 | **4** | 0 |
| `Control loop missed` | 4 | 다수 | **0** |
| 목표 결과 | 2/3 성공 | **0/3** | (아래 §7) |

F3 는 **102 m 를 헤매다**(우회율 6.3) 충돌 1틱을 냈습니다.

**교훈**: 계산상 0.05 s 차이가 실제로는 추종 붕괴 → 재계획 폭풍으로 번졌습니다.
제어 주파수는 "이 정도면 되겠지" 로 낮출 값이 아닙니다.

**채택한 것**: `model_dt 0.10 → 0.05`, `time_steps 63 → 126`. 지평선 6.30 s 를 그대로
두고 20 Hz 를 유지하면서 shifting 을 켰습니다. 대가는 롤아웃 스텝 2배인데,
개발 PC 에서 `controller_server` CPU **7.3%** 로 여유가 있었습니다.
⚠ **Orin Nano 는 미확인**입니다. 포화되면 `batch_size` 1600 → 1000 을 먼저 낮추세요
(지평선을 지키는 편이 낫습니다).

> ##관찰## 이번 건은 §8.9(실행편차)와 성격이 다릅니다. 편차로는 설명이 안 되는
> 규모(BT 경고 0 → 815)라 단일 실행으로도 판정할 수 있었습니다. **연속량이 아니라
> 계수(count)로 나타나는 지표는 잡음에 훨씬 강합니다** — A/B 를 볼 때 참고하세요.

### 🔬 8.9 이 하네스의 실행편차 — **단일 실행 비교는 못 믿는다**

이번 작업에서 가장 값진 발견일 수 있습니다. §8.1~8.6 의 판단들이 전부 **단일 실행
비교**였기 때문입니다.

ALIGN 을 켜고 3목표를 돌렸더니 `F2 협착(G4)` 이 **성공(323 s) → ABORT(자세오차 115°)**
로 뒤집혔습니다. 처음엔 ALIGN 의 회귀로 읽었습니다. 그런데 로그의 진입 시각을
목표별 구간에 맞춰보니:

```
F1  1787199009 ~ 1787199096
F2  1787199096 ~ 1787199356      ← 여기서 ABORT
F3  1787199356 ~ 1787199652
ALIGN 진입:  1787199509          ← F3 구간. F2 는 ALIGN 을 한 번도 겪지 않았다
```

**ALIGN 이 한 번도 발동하지 않은 목표가 성공↔실패로 뒤집혔습니다.** 원인은 ALIGN 이
아니라 하네스 자체의 실행편차입니다. MPPI 는 매 틱 1600개 궤적을 **무작위 표본**으로
굴리고, 시뮬은 벽시계로 돌아 틱 타이밍도 매번 다릅니다.

**의미**:

* §8.1~8.6 의 "되돌림" 판정 중 **일부는 노이즈였을 수 있습니다.** 특히 성공/실패가
  갈린 항목(§8.1 min_hold 4.0, §8.6 spin 문턱 0.10)은 재검증 대상입니다.
* 앞으로 이 하네스로 A/B 를 할 때는 **최소 2회 반복**하고, 성공/실패보다
  **연속량**(소요시간·자세오차·우회율)의 경향을 보세요.
* 반복 실행용 스크립트를 만들어 뒀습니다 — `run_ab.sh` (`ALIGN` 환경변수로 토글,
  `REPS` 로 반복 횟수).

**같은 목표가 실행마다 얼마나 흔들리는지** (전부 alm_lab, 설정 변경 없거나 ALIGN 만 토글):

| 실행 | ALIGN | F2 협착 (G4) | F3 하드케이스 (G3) |
|---|---|---|---|
| ① | 코드 이전 | ✅ 323.3 s / 11.0° | ✅ 174.1 s / 2.0° |
| ② | on | ❌ ABORT / 115.4° | ✅ 295.9 s / 0.2° |
| ③ | off | ✅ 163.4 s / 4.0° | ❌ ABORT / 89.8° |
| ④ | on | ⚠ TIMEOUT / 143.0° | ✅ 206.5 s / 2.1° |

**F2 는 323 → ABORT → 163 s → TIMEOUT, F3 는 성공/성공/실패/성공.**
그리고 결정적으로 **③④ 실행에서 ALIGN 은 한 번도 발동하지 않았습니다**(로그 확인).
즉 위 표의 좌우 차이는 전부 ALIGN 과 무관한 잡음입니다.

이 폭 안에서는 어떤 파라미터 변경도 "좋아졌다/나빠졌다"를 단일 실행으로 말할 수
없습니다. **그리고 이 하네스로는 ALIGN 의 이득도 증명할 수 없습니다** — ALIGN 이
개입하는 상황(경로가 있는데 헤딩만 크게 어긋남)이 640 s 에 1회 수준으로 드문데,
잡음은 그보다 훨씬 크기 때문입니다.

> ⚠ ④의 F2 는 `TIMEOUT` 인데 `elapsed 1621.9 s` 로 설정 상한(400 s)을 크게 넘겼습니다.
> 하네스 자체의 이상입니다(액션 결과 대기 루프가 풀리지 않음). 숫자로 쓰지 마세요.

> 이 항목은 **되돌린 실수**가 아니라 **방법론의 결함**입니다. 그래서 ❌ 가 아니라
> 🔬 로 표시합니다.

## 9. 남은 일

### 9.1 ⭐ `S_정지` 실측 — 실차 나가기 전 필수

**자율주행 시작 시 자동으로 재는 게 아닙니다.** 업링크가 없어 자동 측정이 불가능하고,
실차 처음 켤 때 **스톱워치 1분**이면 끝나는 벤치 확인입니다.

```bash
# ① command_manager 를 끈다 (안 끄면 /mcu/command 가 충돌한다). mcu_bridge 만 띄운다.
# ② 평평한 바닥에 내려놓는다.  ★ 잭업 금지
# ③ 앞바퀴에 테이프로 눈금 표시
ros2 run alm_bringup steer_bench.py            # 안내대로 Enter 만 치면 된다
```

`steer_bench.py` 가 `-30° → +30°` 스텝을 3회 쏘고, 바퀴가 멈추는 순간 Enter 를 치면
`S_정지 = 60° ÷ 시간` 을 계산해 **아래 세 파라미터가 충분한지까지 판정**합니다.
구동 명령은 항상 `speed_rpm = 0` 이라 바퀴는 굴러가지 않습니다.

> ★ **손으로 돌려보는 것은 이 측정이 아닙니다.** 재려는 건 조향축이 자유롭게 도는지가
> 아니라 **조향 모터가 접지 마찰을 이기고 얼마나 빨리 돌리는가** 입니다. 반드시
> 명령을 실어서 모터가 돌리게 해야 합니다.

**정확한 숫자보다 세 가지 답이 필요합니다:**

| 질문 | 왜 |
|---|---|
| **도는가?** | 정지 상태에서 스톨하면 "굴리면서만 조향" 이라는 설계 제약이 생김. dwell 로는 못 메운다 |
| **60° 를 5 s 안에?** | `startup_steer_align_sec: 5.0` → **S_정지 ≥ 12 deg/s** 필요 |
| **77° 를 4 s 안에?** | `mode_switch_dwell_sec: 4.0` (normal↔spin) → **S_정지 ≥ 19.3 deg/s** 필요 |
| (crab 을 쓸 거면) 120° 를? | normal↔crab. 현재 dwell 로는 **S_정지 ≥ 30 deg/s** 필요 |

> ⚠ **정지 상태가 가장 느립니다** — 접지면 전체를 비틀어야 하므로. 잭업(무부하)에서
> 재면 실제보다 훨씬 빠르게 나오고, 그 값으로 dwell 을 정하면 조향이 안 펴진 채
> 출발합니다.

**주행 중 슬루율은 급하지 않습니다** — 명령을 20 deg/s 로 낮춰뒀는데 어떤 조향
모터도 이건 넘습니다. 실측하면 비(100 deg/m)를 유지한 채 속도와 함께 올려 성능을
되찾는 것뿐입니다.

**측정 도구:**
- `alm_bringup/scripts/steer_bench.py` — ⭐ 위 벤치 절차 자동화 (정지 상태)
- `steering_observer` — 주행 중 실효 조향각을 곡률에서 역산 (업링크 불필요)
- 업링크가 생기면: `/mcu/command.steer_deg` 와 `/mcu/state` 를 함께 로깅 →
  상호상관으로 지연 τ 까지 나온다

### 9.2 STM32 업링크 요청 — 11바이트만 먼저

63바이트 전체를 기다리지 말고 `msg_type = 0x03` "MiniState" 를 새로 정의해
**11바이트만** 먼저 받는 걸 제안합니다. 기존 `0x02`(63 B) 규격은 그대로 두면
나중에 갈아탈 때 재작업이 없습니다.

| 순위 | 필드 | 바이트 | 이게 있으면 |
|---|---|---|---|
| **1** | `status_flags` + `fault_code` | 3 | **지금 죽은 안전장치를 살림** |
| **2** | `steer_actual_deg` | 4 | **정지 중에도 유효** — 관측기의 사각지대를 메움 |
| **3** | `ack_sequence` | 4 | 프레임 유실·CRC 실패 검출 |

**먼저 물어볼 것** (답에 따라 설계가 갈립니다):

| # | 질문 | 왜 |
|---|---|---|
| Q1 | **조향 액추에이터가 뭔가요?** 스텝/서보/DC+포텐쇼 | **스텝모터면 스텝 카운트가 곧 각도** — 센서 없이 정확히 앎 |
| Q2 | STM32 가 조향 명령을 **내부에서 램프**하나요? | 한다면 그 setpoint 만 올려줘도 됨 |
| Q3 | 조향 최대 각속도 [deg/s]? / Q4 풀락→반대 풀락 몇 초? | §9.1 |
| Q5 | `mode 0`(정지)이 coast 인가 brake 인가 | `cmd_timeout` 거동 |
| Q6 | `CONS(4)` 직진 판정 각도 실제 값 | `.slx` 에 저장 안 돼 있음 |

### 9.3 그 밖

| # | 항목 | 상태 |
|---|---|---|
| A | `front_x`/`rear_x` 배분 CAD 확인 | 휠베이스 **합**만 CONS 로 확정. 배분이 다르면 URDF 2줄 + footprint 함께 수정 |
| B | `##CONFIRM##` 상수 (`wheel_radius`, `gear_ratio`, `straight_angle`, 부호 2개) | 관측기의 부수효과로 대부분 풀림 |
| C | `/Odometry.twist` = 0 | MPPI 가 매 주기 **"나는 정지 상태"** 로 믿고 롤아웃한다. pose 수치미분은 10 Hz LiDAR 잡음 때문에 득실 불분명해 안 고쳤다. **시뮬이 이 조건을 그대로 재현**하므로 지금까지의 측정에 이미 포함돼 있다 — §6.10 |
| D | LiDAR degeneracy fallback 없음 | odometry 소스가 LiDAR 하나뿐 |
| E | 장애물 예측 없음 | `ObstacleLayer` 에 시간 개념 없음. 범위 큼 |
| ~~F~~ | ~~`model_dt` > 제어주기~~ | ✅ **해결** — `model_dt` 0.10 → 0.05, `time_steps` 63 → 126 (20 Hz 유지). `Control sequence shifting is ON` 확인. ⚠ `controller_frequency` 10 Hz 로 맞추는 안은 **회귀가 나서 되돌림** — §8.10 |
| G | **실차 주행** | ❌ **미검증** |
| ⭐O | **LiDAR 마운트 TF 실측** | `lidar_x/y/z` + roll/pitch/yaw 가 전부 추정값(`0,0,0.5`,회전 0). **줄자면 됩니다.** 이게 틀리면 조향·계획을 아무리 고쳐도 소용없습니다 — §6.10-A |
| P | 후방 사각지대 운용 결정 | 마스크 1.5 m 를 적재물 최소 크기로 줄일지. 줄이면 사각지대↓ 대신 FAST-LIO 정합에 적재물이 섞일 위험↑. `mask_debug` 로 눈으로 보고 정할 것 — §6.10-B |
| Q | `reverse_penalty: 3.0` 재검토 | 지금 값은 **시간 비용**(전/후진 속도비 2.86)에서 나왔습니다. 후방이 사각지대라는 **위험 비용**은 안 들어가 있습니다. 올릴지는 실차에서 후진 빈도를 보고 판단 |
| ~~H~~ | ~~`goal_yaw_aligner.py` 제거 검토~~ | ✅ **삭제 완료** (§6.10-D) — 어떤 런치도 안 띄우고 있었고 ALIGN 이 대체 |
| I | §8.1 · §8.6 재검증 | §8.9 의 실행편차 발견으로 **되돌림 판정의 근거가 약해졌다.** 두 항목 모두 성공/실패로 갈렸는데, 그 지표가 가장 노이즈에 취약하다. `run_ab.sh` 로 반복 실행해 다시 판단할 것 |
| K | ⭐ **포켓형 목표가 계획 불가** (§7.3) | 협착맵 실측: 시작 헤딩 0°/−45°/−90° 모두 `no valid path found`, `replans: 0`. **헤딩 문제가 아니다.** 의심 순서: ① `inflation_radius` 1.2 m vs 포켓 폭 2.8 m (중앙조차 팽창비용이 높다) ② `max_planning_time` 1.0 s ③ Hybrid-A* 각도 이산화(`angle_quantization_bins`). 하나씩 분리해서 볼 것 |
| L | `mode_switch_dwell_sec` 를 3.0 → **4.0** 으로 올림 | 기구학이 요구한 값이다: 최악 스윕 normal 풀락(30°) → spin(47°) = 77°, ÷20 deg/s = **3.85 s**. 3.0 은 부족했다. crab(90°)은 6.0 s 가 필요하지만 자율주행에서 안 쓰므로 덮지 않았다 |
| M | 모드 전환 순간 2 cm 슬립 | 전환 시 `mode_id` 는 즉시 나가는데 `vx` 는 가속제한(1.0 m/s²)으로 램프 하강 → **0.2 s(약 2 cm) 동안 조향축이 도는 중에 굴러감**. 현재 속도(0.20 m/s)에서는 무시 가능. 없애려면 `out_vx` 가 0 이 된 **뒤에** `mode_id` 를 바꿀 것. 속도를 올리면 비례해 커진다 |
| N | 모드 전환 실측 | dwell 4.0 s 는 **계산값**(77° ÷ 20 deg/s). 로봇을 세워두고 `mode_id` 만 바꿔 바퀴가 새 자세로 다 돌아가는 시간을 재면 그 가정이 필요 없어진다. ALIGN 1회 비용(10 s 중 8 s 가 dwell)이 여기 달려 있다 |
| J | ALIGN 실차 문턱 재조정 | `align_enter_deg` 60° 는 **시뮬 분포**로 정했다. 실차는 측위 잡음이 더 크므로 헤딩오차 분포가 넓어질 수 있다. `heading_probe.py` 를 실차에서 한 번 돌려 분포를 다시 볼 것 |

---

## 10. 재현 · 검증 명령

```bash
# 기구학 자체 검증 (ROS 불필요, 9항목)
python3 ALM_auto_ws/src/alm_base_control/scripts/fourwis_encode.py

# ALIGN 기동 자체 검증 (ROS 불필요, 19항목)
#   왕복 방지(래치·히스테리시스·쿨다운)와 후진 오인 방지가 여기서 걸린다
python3 ALM_auto_ws/src/alm_base_control/scripts/path_align.py

# 전 구간 정합성 검사 (기구 상수 ↔ Nav2 ↔ URDF ↔ lattice)
ros2 run alm_navigation nav2_kinematic_check.py     # 0 = 전부 일치

# lattice control set 다시 굽기 (기구 상수를 바꿨을 때)
#   alm_navigation/lattice_primitives/README.md 참고

# ⭐ 정지 조향 슬루율 S_정지 벤치 (실차, command_manager 를 끄고)
ros2 run alm_bringup steer_bench.py
ros2 run alm_bringup steer_bench.py --amp 30 --repeats 3   # 기본값

# 조향 관측 (주행 중, 업링크 불필요)
ros2 run alm_base_control steering_observer.py
ros2 topic echo /steer/observed        # [명령δ, 관측δ, 간극, κ, Δs, 유효]

# ⭐ 자율주행 기동 + 관측용 RViz (경로 · 코스트맵 · footprint · 2D Goal Pose)
ros2 launch alm_bringup navigation.launch.py rviz:=true
#   맵을 바꾸려면 maps/active.yaml 을 고치거나 인자로:
#     map:=<ws>/src/alm_navigation/maps/<맵>/grid.yaml \
#     map_pcd:=.../cloud.pcd  fpfh_db_prefix:=.../fpfh_map

# 하드웨어 없이 통합 기동
ros2 launch alm_bringup webui_dev.launch.py

# 조향 슬루율 민감도 실험
ros2 run alm_bringup fake_mcu.py --ros-args -p steer_rate_deg_s:=30.0
ros2 run alm_bringup fake_mcu.py --ros-args -p publish_state:=false   # 업링크 없는 실차 조건

# 상태 확인
ros2 topic echo /mcu/command --field steer_deg
ros2 topic echo /drive_mode/effective
ros2 topic echo /Odometry --field twist.twist    # 0 인지 확인

# ALIGN 이 실제로 걸리는지 (주행 중)
ros2 param get /command_manager align_enabled
ros2 run alm_base_control command_manager.py --ros-args -p align_enabled:=false  # A/B 비교용
#   로그에 남는 것:
#     ALIGN 진입: 경로 헤딩오차 -62.4° -> 목표 헤딩 +30.0° 로 제자리 회전
#     ALIGN 종료(정렬 완료): 남은 오차 -14.9° -> normal 복귀
```

> **시뮬레이션 하네스**는 scratchpad 에 있고 레포에 넣지 않았습니다
> (`sim_world.py`, `probe_costmap.py`, `run_goals.py`, `start_stack.sh`,
> `heading_probe.py`, `run_ab.sh`). 재작성한다면 §8.7 의 함정 5개를 먼저 보고,
> **§8.9 의 실행편차**를 반드시 감안하세요 — 단일 실행 비교는 못 믿습니다.

---

## 10.1 RViz 로 주행을 보는 법

`alm_navigation/rviz/navigation.rviz` (신규). 측위 확인용 `localization.rviz` 에는
경로도 코스트맵도 없어서 따로 만들었습니다.

```bash
ros2 launch alm_bringup navigation.launch.py rviz:=true
#   또는 이미 떠 있는 스택에 붙일 때
rviz2 -d $(ros2 pkg prefix alm_navigation)/share/alm_navigation/rviz/navigation.rviz
```

**무엇이 보이나**

| 표시 | 토픽 | 색 | 뜻 |
|---|---|---|---|
| Global Costmap | `/global_costmap/costmap` | 코스트맵 | 플래너가 보는 비용. `inflation_radius` 1.2 m 가 여기 |
| **Downsampled Costmap** | `/downsampled_costmap` | 코스트맵 | ★ **Hybrid-A\* 가 실제로 탐색하는 격자.** `downsample_costmap: true` 라 위보다 성깁니다. "왜 여기로 못 가지?" 는 이걸 봐야 합니다 (기본 꺼짐) |
| Local Costmap | `/local_costmap/costmap` | 코스트맵 | MPPI 가 보는 것. `static_layer` 없이 `obstacle_layer` 만이라 **`/scan` 이 죽으면 벽이 사라집니다** |
| Global Plan | `/plan` | 하늘 | 평활화 **전**. pose 축(Axes)으로 표시 — **ALIGN 이 보는 헤딩이 이것** |
| Smoothed Plan | `/plan_smoothed` | 노랑 | ConstrainedSmoother 결과 |
| MPPI 추종 구간 | `/transformed_global_plan` | 분홍 | MPPI 가 물고 있는 조각 (`prune_distance` 2.0 m) |
| Footprint | `/local_costmap/published_footprint` | 초록 | 실제 차체 다각형 |
| LaserScan | `/scan` | 빨강 | |

**목표 주기**: 상단 `2D Goal Pose` 툴 → 지도에서 **드래그**. 드래그 방향이 곧
**도착 자세**입니다 — 클릭만 하면 yaw 가 0 이 되어 엉뚱한 접근 자세를 요구하게 됩니다.

**측위가 틀어졌을 때**: `2D Pose Estimate` 로 대략 위치를 주면
FPFH/TEASER++ 자동 초기화가 다시 수렴합니다.

**보면서 판단하는 법**

| 증상 | 어디를 보나 |
|---|---|
| 경로가 아예 안 그려짐 | 플래너 실패. `Downsampled Costmap` 에서 목표가 팽창영역에 잠겼는지 |
| 경로는 있는데 안 따라감 | `Local Costmap` 에 없는 벽이 있는지 / `Footprint` 가 벽에 닿는지 |
| 제자리에서 도는 중 | `command_manager` 로그의 `ALIGN 진입` 또는 `auto -> spin` |
| 자꾸 멈춤 | `/scan` 이 끊겼는지 (Local Costmap 이 비어 보임) |

> MPPI 후보 궤적(`/trajectories`)은 `nav2.yaml` 의 `FollowPath.visualize: true` 라야
> 나옵니다. 기본 `false` — CPU 를 씁니다. 디버깅할 때만 켜세요.

---

## 11. 파일 지도

```
경로계획
  alm_navigation/config/nav2.yaml                    모든 Nav2 파라미터 (유도 근거를 주석에)
  alm_navigation/behavior_trees/navigate_*.xml       SmoothPath + IsPathValid + 플래너 2단
  alm_navigation/lattice_primitives/                 ★ 이 플랫폼 전용 control set + 생성법
  alm_navigation/launch/navigation.launch.py         BT·lattice 절대경로 주입
  alm_navigation/rviz/navigation.rviz                ★ 주행 관측용 (경로·코스트맵·목표툴)
  alm_navigation/rviz/localization.rviz              측위 확인용 (경로/코스트맵 없음)
  alm_navigation/scripts/nav2_kinematic_check.py     정합성 검사기 (7개 섹션, §7=ALIGN)

제어
  alm_base_control/scripts/fourwis_encode.py    ★ 기구학 단일 진실 공급원
                                                  (R_min, twist↔조향각, 슬루 제한)
  alm_base_control/scripts/path_align.py        ★ ALIGN 기동 — 전역 경로가 표현 못 하는
                                                  제자리 회전을 제어단이 메운다 (§6.8)
  alm_base_control/scripts/command_manager.py     모드 해석 · 안전 게이팅 · 조향 응답 · ALIGN
  alm_base_control/scripts/steering_observer.py   곡률 기반 조향 관측 (로깅 전용)
  alm_base_control/scripts/cmd_arbiter.py         자율/텔레옵 동작권
  alm_base_control/config/base_control.yaml       기구 상수 (STM32 CONS 와 일치 필수)

하드웨어 인터페이스
  alm_mcu_interface/scripts/mcu_bridge.py       UART 전송 계층 (기구학 없음)
  alm_mcu_interface/docs/uart_protocol.md       프레임 규격 v2 (업링크 미구현 명시)
  alm_bringup/scripts/fake_mcu.py               하드웨어 없이 테스트 (조향 모델 포함)

측위
  alm_navigation/launch/localization.launch.py       FAST-LIO + FPFH/TEASER++ ICP
  thirdparty/.../FAST_LIO/src/laserMapping.cpp       twist 미발행 지점
```

---

## 12. 용어집

| 용어 | 뜻 |
|---|---|
| **R_min** | 최소 선회반경. 이 플랫폼 1.643 m |
| **ICR** | 순간회전중심 |
| **κ (곡률)** | `1/R = dθ/ds`. 조향각과 일대일. **시간이 아니라 거리로 정의됨** |
| **RWS** | 후륜 보조조향. 여기선 앞바퀴의 50% 역방향 |
| **슬루율** | 액추에이터의 최대 변화 속도 [deg/s] |
| **dwell** | 조향축이 자리 잡을 때까지 의도적으로 멈춰 기다리는 시간 |
| **footprint** | 충돌검사용 차체 외곽. x[-0.65, 0.72], y[±0.53] |
| **내접/외접** | 0.530 / 0.894 m. `inflation_radius` 는 외접보다 커야 함 |
| **control set** | Lattice 플래너가 쓰는 모션 프리미티브 집합 |
| **BT** | Behavior Tree |
| **critic** | MPPI 가 궤적을 평가하는 비용 함수 조각 |
